(()=>{
'use strict';
const KEY='masar.v3.state';
const icons={
 grid:'<svg viewBox="0 0 24 24"><rect x="4" y="4" width="6" height="6" rx="1"/><rect x="14" y="4" width="6" height="6" rx="1"/><rect x="4" y="14" width="6" height="6" rx="1"/><rect x="14" y="14" width="6" height="6" rx="1"/></svg>',
 calendar:'<svg viewBox="0 0 24 24"><rect x="3.5" y="5" width="17" height="16" rx="2"/><path d="M8 3v4M16 3v4M3.5 10h17"/></svg>',
 book:'<svg viewBox="0 0 24 24"><path d="M5 4.5A2.5 2.5 0 0 1 7.5 2H20v18H7.5A2.5 2.5 0 0 0 5 22V4.5Z"/><path d="M5 4.5V22M9 7h7M9 11h7"/></svg>',
 exam:'<svg viewBox="0 0 24 24"><path d="M7 3h10v4H7zM5 7h14v14H5z"/><path d="m8 14 2.2 2.2L16 10.5"/></svg>',
 tasks:'<svg viewBox="0 0 24 24"><path d="M9 6h11M9 12h11M9 18h11"/><path d="m4 6 1 1 2-2M4 12l1 1 2-2M4 18l1 1 2-2"/></svg>',
 chart:'<svg viewBox="0 0 24 24"><path d="M4 19V5M4 19h17"/><path d="m7 15 4-4 3 2 5-6"/></svg>',
 settings:'<svg viewBox="0 0 24 24"><circle cx="12" cy="12" r="3"/><path d="M19.4 15a1.7 1.7 0 0 0 .3 1.9l.1.1-1.8 1.8-.1-.1a1.7 1.7 0 0 0-1.9-.3 1.7 1.7 0 0 0-1 1.6V21h-2.6v-.9a1.7 1.7 0 0 0-1-1.6 1.7 1.7 0 0 0-1.9.3l-.1.1-1.8-1.8.1-.1A1.7 1.7 0 0 0 8 15a1.7 1.7 0 0 0-1.6-1H5.5v-2.6h.9A1.7 1.7 0 0 0 8 10a1.7 1.7 0 0 0-.3-1.9l-.1-.1 1.8-1.8.1.1a1.7 1.7 0 0 0 1.9.3 1.7 1.7 0 0 0 1.9-1.6V4h2.6v.9a1.7 1.7 0 0 0 1 1.6 1.7 1.7 0 0 0 1.9-.3l.1-.1 1.8 1.8-.1.1a1.7 1.7 0 0 0-.3 1.9 1.7 1.7 0 0 0-.3 1.9 1.7 1.7 0 0 0 1.6 1h.9V14h-.9a1.7 1.7 0 0 0-1.6 1Z"/></svg>',
 play:'<svg viewBox="0 0 24 24"><circle cx="12" cy="12" r="9"/><path d="m10 8 6 4-6 4V8Z"/></svg>',
 plus:'<svg viewBox="0 0 24 24"><path d="M12 5v14M5 12h14"/></svg>',
 search:'<svg viewBox="0 0 24 24"><circle cx="11" cy="11" r="6.5"/><path d="m16 16 4 4"/></svg>',
 bell:'<svg viewBox="0 0 24 24"><path d="M18 9a6 6 0 0 0-12 0c0 7-3 7-3 9h18c0-2-3-2-3-9M10 21h4"/></svg>',
 moon:'<svg viewBox="0 0 24 24"><path d="M20 15.5A8.5 8.5 0 0 1 8.5 4 8.5 8.5 0 1 0 20 15.5Z"/></svg>',
 sun:'<svg viewBox="0 0 24 24"><circle cx="12" cy="12" r="4"/><path d="M12 2v2M12 20v2M4.9 4.9l1.4 1.4M17.7 17.7l1.4 1.4M2 12h2M20 12h2M4.9 19.1l1.4-1.4M17.7 6.3l1.4-1.4"/></svg>',
 close:'<svg viewBox="0 0 24 24"><path d="m6 6 12 12M18 6 6 18"/></svg>',
 arrow:'<svg viewBox="0 0 24 24"><path d="M5 12h14M13 6l6 6-6 6"/></svg>',
 refresh:'<svg viewBox="0 0 24 24"><path d="M20 11a8 8 0 0 0-14.8-3M4 5v4h4M4 13a8 8 0 0 0 14.8 3M20 19v-4h-4"/></svg>',
 check:'<svg viewBox="0 0 24 24"><path d="m5 12 4 4L19 6"/></svg>',
 more:'<svg viewBox="0 0 24 24" fill="currentColor"><circle cx="5" cy="12" r="1.5"/><circle cx="12" cy="12" r="1.5"/><circle cx="19" cy="12" r="1.5"/></svg>',info:'<svg viewBox="0 0 24 24"><circle cx="12" cy="12" r="9"/><path d="M12 10v6M12 7h.01"/></svg>',
 trash:'<svg viewBox="0 0 24 24"><path d="M4 7h16M9 7V4h6v3M7 7l1 14h8l1-14M10 11v6M14 11v6"/></svg>',
 edit:'<svg viewBox="0 0 24 24"><path d="m4 16-.7 4.7L8 20l11-11-4-4L4 16Z"/><path d="m13.5 6.5 4 4"/></svg>',
 info:'<svg viewBox="0 0 24 24"><circle cx="12" cy="12" r="9"/><path d="M12 10v6M12 7h.01"/></svg>'
};
const I=k=>`<span class="ico">${icons[k]||''}</span>`;
const now=new Date(); now.setHours(0,0,0,0);
const iso=d=>{const x=new Date(d);x.setHours(12,0,0,0);return x.toISOString().slice(0,10)};
const addDays=(d,n)=>{const x=new Date(d);x.setDate(x.getDate()+n);return x};
const todayIso=iso(now);
const daysLeft=d=>Math.ceil((new Date(d+'T23:59:59')-new Date())/86400000);
const fmtDate=d=>new Intl.DateTimeFormat(state.lang==='ar'?'ar-EG':'en-US',{day:'numeric',month:'short'}).format(new Date(d));
const fullDate=d=>new Intl.DateTimeFormat(state.lang==='ar'?'ar-EG':'en-US',{weekday:'long',day:'numeric',month:'long'}).format(new Date(d));
const dayNames=stateLang=>stateLang==='ar'?['الأحد','الاثنين','الثلاثاء','الأربعاء','الخميس','الجمعة','السبت']:['Sunday','Monday','Tuesday','Wednesday','Thursday','Friday','Saturday'];
const seed={name:'سيف',major:'طالب جامعي',lang:'ar',dark:false,streak:12,focusMinutes:245,settings:{notifications:true,autoPlan:true,weeklyReview:true},courses:[
{id:1,code:'STAT 201',name:'الإحصاء الحيوي',teacher:'د. منى علي',progress:48,difficulty:'hard'},
{id:2,code:'BOT 302',name:'وقاية النبات',teacher:'د. سامح فؤاد',progress:63,difficulty:'hard'},
{id:3,code:'BIO 204',name:'علم الأحياء الدقيقة',teacher:'د. أحمد محمود',progress:72,difficulty:'medium'},
{id:4,code:'CHEM 210',name:'الكيمياء العضوية',teacher:'د. خالد حسن',progress:81,difficulty:'medium'}],
exams:[{id:1,course:1,title:'امتحان الإحصاء الحيوي',date:iso(addDays(now,5)),readiness:54},{id:2,course:3,title:'اختبار الأحياء الدقيقة',date:iso(addDays(now,12)),readiness:76},{id:3,course:2,title:'نهائي وقاية النبات',date:iso(addDays(now,21)),readiness:62}],
tasks:[{id:1,title:'مراجعة الاحتمالات والتوزيعات',course:1,due:todayIso,priority:'high',done:false,minutes:50},{id:2,title:'حل 20 سؤال من الفصل الثالث',course:1,due:todayIso,priority:'high',done:false,minutes:50},{id:3,title:'تلخيص محاضرة الفطريات',course:2,due:iso(addDays(now,1)),priority:'medium',done:false,minutes:35},{id:4,title:'قراءة الفصل الخامس',course:3,due:iso(addDays(now,2)),priority:'low',done:false,minutes:30},{id:5,title:'رفع تقرير المعمل',course:4,due:iso(addDays(now,3)),priority:'medium',done:false,minutes:40}],sessions:[],plan:[]};
function load(){try{return merge(seed,JSON.parse(localStorage.getItem(KEY)||'{}'))}catch{return structuredClone(seed)}}
function merge(a,b){const o={...structuredClone(a),...b};for(const k of ['settings'])o[k]={...a[k],...(b[k]||{})};for(const k of ['courses','exams','tasks','sessions','plan'])o[k]=Array.isArray(b[k])?b[k]:a[k];return o}
let state=load();
function save(){localStorage.setItem(KEY,JSON.stringify(state))}
const t={ar:{today:'اليوم',planner:'المخطط',courses:'المواد',exams:'الامتحانات',tasks:'المهام',progress:'التقدم',settings:'الإعدادات',focus:'التركيز',recovery:'إنقاذ الخطة',search:'ابحث في مسار…',greeting:'أهلًا',todaySub:'كل ما تحتاجه اليوم، بترتيب واضح ومن غير زحمة.',what:'ماذا تذاكر الآن؟',why:'لماذا هذه أولويتي؟',start:'ابدأ جلسة',rebuild:'إعادة بناء الخطة',schedule:'جدول اليوم',upcoming:'القادم',planHealth:'صحة الخطة',healthy:'الخطة تحت السيطرة',attention:'تحتاج انتباه',streak:'تتابع أيام',minutes:'دقائق تركيز',tasksToday:'مهام اليوم',examsSoon:'امتحانات قريبة',viewAll:'عرض الكل',add:'إضافة',newCourse:'إضافة مادة',newExam:'إضافة امتحان',newTask:'إضافة مهمة',empty:'لا يوجد شيء هنا بعد',save:'حفظ',cancel:'إلغاء',course:'المادة',title:'العنوان',date:'التاريخ',priority:'الأولوية',high:'عالية',medium:'متوسطة',low:'منخفضة',progress:'التقدم',teacher:'المدرس',name:'الاسم',major:'التخصص',language:'اللغة',appearance:'المظهر',light:'فاتح',dark:'داكن',auto:'تكييف الخطة تلقائيًا',notify:'التنبيهات',weekly:'مراجعة أسبوعية',on:'مفعّل',off:'متوقف',days:'يوم',remaining:'متبقي',done:'منجز',readiness:'الجاهزية',daysTo:'يوم متبقي',recoveryTitle:'اتأخرت عن الخطة؟',recoveryText:'مسار يعيد ترتيب الوقت حول الامتحانات والأولويات بدل تكديس كل شيء في يوم واحد.',move:'إعادة توزيع الجلسات',defer:'تأجيل الأقل أولوية',extend:'فتح مساحة زمنية إضافية',apply:'تطبيق',close:'إغلاق',pause:'إيقاف مؤقت',resume:'استكمال',finish:'إنهاء الجلسة',analysis:'تحليل الخطة',searchHint:'ابحث في المواد والامتحانات والمهام.',progressTitle:'التقدم',coursesTitle:'موادك',examsTitle:'الامتحانات',tasksTitle:'مهامك',settingsTitle:'الإعدادات',plannerTitle:'المخطط الأسبوعي',notifications:'الإشعارات',noNotifications:'أنت محدث بالكامل.',noResults:'لا توجد نتائج',export:'تصدير التقرير',reset:'إعادة بيانات التجربة',edit:'تعديل',delete:'حذف',details:'التفاصيل',difficulty:'الصعوبة',hard:'صعبة',medium:'متوسطة',easy:'سهلة',addSession:'ابدأ',noTasks:'لا توجد مهام مفتوحة.',courseProgress:'تقدم المادة',completed:'مكتمل',planUpdated:'تم تحديث الخطة بناءً على بياناتك.',sessionLogged:'تم تسجيل الجلسة وتحديث تقدمك.',deleted:'تم الحذف.',required:'املأ الحقول المطلوبة.',confirmDelete:'هل تريد حذف هذا العنصر؟',todayEmpty:'يوم هادئ. لا توجد أولوية عاجلة.',calendar:'تقويم',week:'أسبوع',overview:'نظرة عامة',weeklyLoad:'الحمل الأسبوعي',readinessRisk:'مخاطر الامتحانات',focusNow:'التركيز الآن',account:'الحساب',student:'الطالب',resetWarning:'سيتم حذف بياناتك المحلية التجريبية.'},en:{today:'Today',planner:'Planner',courses:'Courses',exams:'Exams',tasks:'Tasks',progress:'Progress',settings:'Settings',focus:'Focus',recovery:'Recovery',search:'Search MASAR…',greeting:'Welcome',todaySub:'Everything you need today, ordered without the noise.',what:'What should you study now?',why:'Why this priority?',start:'Start session',rebuild:'Rebuild plan',schedule:'Today',upcoming:'Upcoming',planHealth:'Plan health',healthy:'On track',attention:'Needs attention',streak:'Day streak',minutes:'Focus minutes',tasksToday:'Today tasks',examsSoon:'Exams soon',viewAll:'View all',add:'Add',newCourse:'Add course',newExam:'Add exam',newTask:'Add task',empty:'Nothing here yet',save:'Save',cancel:'Cancel',course:'Course',title:'Title',date:'Date',priority:'Priority',high:'High',medium:'Medium',low:'Low',progress:'Progress',teacher:'Teacher',name:'Name',major:'Major',language:'Language',appearance:'Appearance',light:'Light',dark:'Dark',auto:'Auto-adapt plan',notify:'Notifications',weekly:'Weekly review',on:'On',off:'Off',days:'days',remaining:'remaining',done:'Done',readiness:'Readiness',daysTo:'days left',recoveryText:'MASAR reshapes time around exams and priorities instead of stacking everything onto one day.',recoveryTitle:'Fell behind?',recoveryText:'MASAR reshapes time around exams and priorities instead of stacking everything onto one day.',move:'Redistribute sessions',defer:'Defer low priority',extend:'Open extra capacity',apply:'Apply',close:'Close',pause:'Pause',resume:'Resume',finish:'Finish session',analysis:'Plan analysis',searchHint:'Search courses, exams and tasks.',progressTitle:'Progress',coursesTitle:'Your courses',examsTitle:'Exams',tasksTitle:'Your tasks',settingsTitle:'Settings',plannerTitle:'Weekly planner',notifications:'Notifications',noNotifications:'You are all caught up.',noResults:'No results',export:'Export report',reset:'Reset demo data',edit:'Edit',delete:'Delete',details:'Details',difficulty:'Difficulty',hard:'Hard',medium:'Medium',easy:'Easy',addSession:'Start',noTasks:'No open tasks.',courseProgress:'Course progress',completed:'Completed',planUpdated:'Plan updated from your data.',sessionLogged:'Session logged and progress updated.',deleted:'Deleted.',required:'Complete the required fields.',confirmDelete:'Delete this item?',todayEmpty:'A calm day. No urgent priority.',calendar:'Calendar',week:'Week',overview:'Overview',weeklyLoad:'Weekly load',readinessRisk:'Exam risk',focusNow:'Focus now',account:'Account',student:'Student',resetWarning:'This clears your local demo data.'}};
const tr=k=>t[state.lang][k]||k;
function setLang(l){state.lang=l;save();document.documentElement.lang=l;document.documentElement.dir=l==='ar'?'rtl':'ltr';render()}
function course(id){return state.courses.find(c=>c.id==id)}
function courseName(id){return course(id)?.name||'—'}
function difficulty(c){return c?.difficulty||'medium'}
function nav(){return [['today','grid'],['planner','calendar'],['courses','book'],['exams','exam'],['tasks','tasks'],['progress','chart'],['settings','settings']]}
function escape(x){return String(x??'').replace(/[&<>'"]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;',"'":'&#39;','"':'&quot;'}[c]))}
function pageHead(title,sub,actions=''){return `<div class="page-head"><div><div class="eyebrow">MASAR / ${escape(title)}</div><h1>${escape(title)}</h1><p>${escape(sub||'')}</p></div><div class="page-actions">${actions}</div></div>`}
function shell(active,body){return `<div class="app-shell"><aside class="sidebar"><div class="brand"><div class="brand-mark">${I('arrow')}</div><div><strong>MASAR</strong><span>Student OS</span></div></div><div class="nav-label">${state.lang==='ar'?'مساحتك':'Workspace'}</div><nav>${nav().map(([k,i])=>`<button class="nav-item ${active===k?'active':''}" data-nav="${k}">${I(i)}<span>${tr(k)}</span></button>`).join('')}</nav><div class="nav-label quick-label">${state.lang==='ar'?'اختصارات':'Quick access'}</div><button class="nav-item" data-action="focus">${I('play')}<span>${tr('focus')}</span><kbd>F</kbd></button><button class="nav-item" data-action="recovery">${I('refresh')}<span>${tr('recovery')}</span></button><div class="sidebar-spacer"></div><div class="student-card"><div class="avatar">${escape((state.name||'م').slice(0,1))}</div><div><strong>${escape(state.name)}</strong><small>${escape(state.major)}</small></div><button class="icon-btn small" data-nav="settings" aria-label="settings">${I('settings')}</button></div></aside><main class="main"><header class="topbar"><div class="mobile-brand"><span class="brand-dot">M</span><strong>MASAR</strong></div><button class="search-trigger" data-action="search">${I('search')}<span>${tr('search')}</span><kbd>⌘ K</kbd></button><div class="top-actions"><button class="icon-btn" data-action="notifications" aria-label="notifications">${I('bell')}<i class="notification-dot"></i></button><button class="icon-btn" data-action="theme" aria-label="theme">${state.dark?I('sun'):I('moon')}</button></div></header>${body}</main><nav class="mobile-nav">${[['today','grid'],['planner','calendar'],['tasks','tasks'],['progress','chart'],['settings','settings']].map(([k,i])=>`<button class="${active===k?'active':''}" data-nav="${k}">${I(i)}<span>${tr(k)}</span></button>`).join('')}</nav><div id="modal-root"></div><div id="focus-root"></div><div id="toast" class="toast"></div></div>`}
function priorityTask(){const open=state.tasks.filter(x=>!x.done);const exams=state.exams.map(e=>({...e,days:daysLeft(e.date)}));return open.sort((a,b)=>{const pa={high:3,medium:2,low:1}[a.priority],pb={high:3,medium:2,low:1}[b.priority];const ea=exams.find(e=>e.course==a.course),eb=exams.find(e=>e.course==b.course);const urgency=e=>e?Math.max(0,30-e.days):0;return (pb+urgency(eb)/20)-(pa+urgency(ea)/20)})[0]}
function todayPage(){const task=priorityTask();const near=[...state.exams].sort((a,b)=>daysLeft(a.date)-daysLeft(b.date))[0];const todayTasks=state.tasks.filter(x=>x.due===todayIso&&!x.done);const completed=state.tasks.filter(x=>x.done).length;const avg=state.courses.length?Math.round(state.courses.reduce((s,c)=>s+c.progress,0)/state.courses.length):0;const risk=state.exams.filter(e=>daysLeft(e.date)<=14&&e.readiness<70).length;return shell('today',`<section class="content"><div class="welcome"><div><div class="eyebrow">${fullDate(now)}</div><h1>${tr('greeting')}، ${escape(state.name)}.</h1><p>${tr('todaySub')}</p></div><div class="welcome-actions"><button class="btn" data-action="recovery">${I('refresh')}${tr('recovery')}</button><button class="btn primary" data-action="rebuild">${I('refresh')}${tr('rebuild')}</button></div></div><div class="hero-grid"><div class="focus-card"><div class="focus-accent"></div><div class="focus-copy"><span class="focus-label">${tr('what')}</span><h2>${task?escape(task.title):tr('todayEmpty')}</h2><p>${task?`${escape(courseName(task.course))} · ${task.minutes||50} ${state.lang==='ar'?'دقيقة':'min'} · ${tr(task.priority)}`:tr('todayEmpty')}</p><div class="focus-reason"><span>${I('info')}</span><div><strong>${tr('why')}</strong><small>${task?reasonFor(task):state.lang==='ar'?'أنجز مهامك أو أضف خطة جديدة ليحدد مسار الأولوية التالية.':'Complete your tasks or add a plan and MASAR will surface the next priority.'}</small></div></div><div class="focus-cta"><button class="btn primary large" data-action="focus" ${task?'':'disabled'}>${I('play')}${tr('start')}</button><button class="text-btn" data-nav="planner">${tr('viewAll')} ${I('arrow')}</button></div></div><div class="focus-visual"><div class="ring"><span>${task?'50':'—'}</span><small>${task?(state.lang==='ar'?'دقيقة':'min'):''}</small></div><div class="focus-mini">${task?state.lang==='ar'?'جلسة واحدة تكفي كبداية.':'One focused session is enough to start.':tr('todayEmpty')}</div></div></div><div class="side-stack"><div class="metric-card"><div class="metric-icon">${I('tasks')}</div><span>${tr('tasksToday')}</span><strong>${todayTasks.length}</strong><small>${state.tasks.filter(x=>!x.done).length} ${tr('remaining')}</small></div><div class="metric-card"><div class="metric-icon">${I('exam')}</div><span>${tr('examsSoon')}</span><strong>${risk}</strong><small>${near?fmtDate(near.date):'—'}</small></div><div class="metric-card"><div class="metric-icon">${I('chart')}</div><span>${tr('courseProgress')}</span><strong>${avg}%</strong><small>${completed} ${tr('done')}</small></div></div></div><div class="section-grid"><div class="panel"><div class="panel-head"><div><h3>${tr('schedule')}</h3><p>${fullDate(now)}</p></div><button class="text-btn" data-nav="planner">${tr('viewAll')} ${I('arrow')}</button></div>${todayTimeline()}</div><div class="panel health"><div class="panel-head"><div><h3>${tr('planHealth')}</h3><p>${avg>=60?tr('healthy'):tr('attention')}</p></div><span class="health-score ${avg>=60?'good':'warn'}">${avg}%</span></div><div class="bar"><span style="width:${avg}%"></span></div><div class="health-grid"><div><strong>${state.tasks.filter(x=>!x.done).length}</strong><span>${tr('remaining')}</span></div><div><strong>${state.focusMinutes}</strong><span>${tr('minutes')}</span></div><div><strong>${state.streak}</strong><span>${tr('streak')}</span></div></div><button class="outline-btn full" data-action="analysis">${I('chart')}${tr('analysis')}</button></div></div><div class="section-grid lower"><div class="panel"><div class="panel-head"><div><h3>${tr('upcoming')}</h3><p>${tr('examsSoon')}</p></div><button class="text-btn" data-nav="exams">${tr('viewAll')} ${I('arrow')}</button></div>${state.exams.slice().sort((a,b)=>daysLeft(a.date)-daysLeft(b.date)).slice(0,3).map(examRow).join('')}</div><div class="panel"><div class="panel-head"><div><h3>${state.lang==='ar'?'المهام التالية':'Next tasks'}</h3><p>${state.lang==='ar'?'ما يستحق وقتك بعد ذلك.':'What deserves your next block.'}</p></div><button class="text-btn" data-nav="tasks">${tr('viewAll')} ${I('arrow')}</button></div>${state.tasks.filter(x=>!x.done).slice(0,3).map(taskRow).join('')}</div></div></section>`)}
function reasonFor(task){const e=state.exams.find(x=>x.course==task.course);if(e&&daysLeft(e.date)<=7)return state.lang==='ar'?`امتحان ${courseName(task.course)} بعد ${Math.max(0,daysLeft(e.date))} أيام، والجاهزية ${e.readiness}%.`:`The ${courseName(task.course)} exam is in ${Math.max(0,daysLeft(e.date))} days at ${e.readiness}% readiness.`;if(task.priority==='high')return state.lang==='ar'?'مهمة عالية الأولوية ومستحقة اليوم.':'High priority and due today.';return state.lang==='ar'?'هذه أفضل خطوة تالية بناءً على الأولوية والموعد.':'Best next step based on priority and deadline.'}
function todayTimeline(){const list=state.tasks.filter(x=>x.due===todayIso).slice(0,5);if(!list.length)return `<div class="empty-state compact">${I('check')}<div><strong>${tr('todayEmpty')}</strong><span>${state.lang==='ar'?'استمتع بالمساحة الفارغة أو أضف مهمة.':'Enjoy the breathing room or add a task.'}</span></div></div>`;return `<div class="timeline">${list.map((x,i)=>`<div class="timeline-item"><div class="time-dot ${x.done?'done':''}">${x.done?I('check'):String(i+1)}</div><div class="timeline-content"><div><strong class="${x.done?'done-text':''}">${escape(x.title)}</strong><small>${escape(courseName(x.course))} · ${x.minutes||50} ${state.lang==='ar'?'دقيقة':'min'}</small></div><button class="check-btn ${x.done?'checked':''}" data-task-toggle="${x.id}">${x.done?I('check'):''}</button></div></div>`).join('')}</div>`}
function examRow(e){const d=daysLeft(e.date);return `<div class="list-row interactive" data-exam="${e.id}"><div class="list-icon exam-icon">${I('exam')}</div><div class="list-content"><strong>${escape(e.title)}</strong><small>${escape(courseName(e.course))} · ${fmtDate(e.date)}</small></div><div class="readiness"><span>${e.readiness}%</span><small>${d<0?'—':d+' '+tr('daysTo')}</small></div>${I('arrow')}</div>`}
function taskRow(x){return `<div class="list-row task-row"><button class="check-btn ${x.done?'checked':''}" data-task-toggle="${x.id}">${x.done?I('check'):''}</button><div class="list-content"><strong class="${x.done?'done-text':''}">${escape(x.title)}</strong><small>${escape(courseName(x.course))} · ${fmtDate(x.due)}</small></div><span class="priority ${x.priority}">${tr(x.priority)}</span><button class="icon-btn small" data-menu="task:${x.id}">${I('more')}</button></div>`}
function plannerPage(){const base=new Date(now);base.setDate(base.getDate()-base.getDay());const days=Array.from({length:7},(_,i)=>addDays(base,i));return shell('planner',`<section class="content">${pageHead(tr('plannerTitle'),state.lang==='ar'?'الخطة تتغير مع إنجازك وتأخرك، وليست جدولًا ثابتًا.':'A weekly plan that adapts as you complete work or fall behind.','<button class="btn" data-action="analysis">'+I('chart')+tr('analysis')+'</button><button class="btn primary" data-action="rebuild">'+I('refresh')+tr('rebuild')+'</button>')}<div class="planner-toolbar"><div class="week-switch"><button class="icon-btn small">${I('arrow')}</button><strong>${fmtDate(days[0])} — ${fmtDate(days[6])}</strong><button class="icon-btn small">${I('arrow')}</button></div><span class="plan-note">${I('info')}${state.lang==='ar'?'كل جلسة مرتبطة بمهمة أو امتحان.':'Every block maps to a task or exam.'}</span></div><div class="calendar-grid"><div class="calendar-corner"></div>${days.map(d=>`<div class="calendar-day ${iso(d)===todayIso?'today':''}"><small>${dayNames(state.lang)[d.getDay()]}</small><strong>${d.getDate()}</strong></div>`).join('')}${['08:00','10:00','12:00','14:00','16:00','18:00'].map((time,r)=>`<div class="calendar-time">${time}</div>${days.map((d,i)=>`<div class="calendar-slot">${planEventFor(d,r)}</div>`).join('')}`).join('')}</div><div class="section-grid planner-bottom"><div class="panel"><div class="panel-head"><div><h3>${tr('analysis')}</h3><p>${state.lang==='ar'?'لماذا تتغير الأولويات؟':'Why priorities change'}</p></div></div>${analysisPoints()}</div><div class="panel"><div class="panel-head"><div><h3>${tr('recovery')}</h3><p>${tr('recoveryText')}</p></div></div><button class="btn primary" data-action="recovery">${I('refresh')}${tr('recovery')}</button></div></div></section>`)}
function planEventFor(d,r){const date=iso(d);const task=state.plan.find(p=>p.date===date&&p.slot===r);if(!task)return '';const cls=task.priority==='high'?'urgent':task.priority==='low'?'soft':'normal';return `<button class="calendar-event ${cls}" data-plan-task="${task.taskId}"><strong>${escape(task.title)}</strong><small>${task.minutes||50} min</small></button>`}
function analysisPoints(){const exams=[...state.exams].sort((a,b)=>daysLeft(a.date)-daysLeft(b.date));const e=exams[0];return `<div class="insight-list"><div><span>01</span><p>${e?state.lang==='ar'?`الامتحان الأقرب هو <b>${escape(e.title)}</b> خلال ${Math.max(0,daysLeft(e.date))} أيام.`:`${escape(e.title)} is the nearest exam in ${Math.max(0,daysLeft(e.date))} days.`:tr('empty')}</p></div><div><span>02</span><p>${state.lang==='ar'?'الجاهزية المنخفضة ترفع وزن المادة حتى لا تتفاجأ قبل الامتحان.':'Lower readiness increases a course’s weight before the exam.'}</p></div><div><span>03</span><p>${state.lang==='ar'?'يتم ترك مساحة احتياطية بدل ملء اليوم بالكامل.':'Buffer capacity is kept instead of filling every hour.'}</p></div></div>`}
function coursesPage(){return shell('courses',`<section class="content">${pageHead(tr('coursesTitle'),state.lang==='ar'?'تقدم كل مادة وصعوبتها وموعدها تدخل في قرارات الخطة.':'Course progress, difficulty and deadlines shape your plan.','<button class="btn primary" data-action="course">'+I('plus')+tr('newCourse')+'</button>')}<div class="course-grid">${state.courses.map(c=>`<article class="course-card panel" data-course="${c.id}"><div class="course-top"><span class="course-code">${escape(c.code)}</span><button class="icon-btn small" data-menu="course:${c.id}">${I('more')}</button></div><h3>${escape(c.name)}</h3><p>${escape(c.teacher)}</p><div class="course-meta"><span>${tr('difficulty')}: ${tr(difficulty(c))}</span><strong>${c.progress}%</strong></div><div class="bar"><span style="width:${c.progress}%"></span></div><div class="course-foot"><span>${state.tasks.filter(x=>x.course==c.id&&!x.done).length} ${tr('remaining')}</span><button class="text-btn" data-course-open="${c.id}">${tr('details')} ${I('arrow')}</button></div></article>`).join('')}</div></section>`)}
function examsPage(){return shell('exams',`<section class="content">${pageHead(tr('examsTitle'),state.lang==='ar'?'الموعد + الجاهزية + حجم العمل يحددون الخطر.':'Dates, readiness and workload determine exam risk.','<button class="btn primary" data-action="exam">'+I('plus')+tr('newExam')+'</button>')}<div class="exam-grid">${state.exams.slice().sort((a,b)=>daysLeft(a.date)-daysLeft(b.date)).map(e=>{const d=daysLeft(e.date);const risk=e.readiness<55?'risk':e.readiness<75?'warn':'good';return `<article class="exam-card panel" data-exam="${e.id}"><div class="exam-top"><span class="status ${risk}">${risk==='risk'?(state.lang==='ar'?'خطر':'Risk'):risk==='warn'?(state.lang==='ar'?'متابعة':'Watch'):(state.lang==='ar'?'مطمئن':'On track')}</span><button class="icon-btn small" data-menu="exam:${e.id}">${I('more')}</button></div><h3>${escape(e.title)}</h3><p>${escape(courseName(e.course))}</p><div class="countdown"><strong>${Math.max(0,d)}</strong><span>${tr('days')}</span><small>${fmtDate(e.date)}</small></div><div class="readiness-line"><div><span>${tr('readiness')}</span><strong>${e.readiness}%</strong></div><div class="bar"><span style="width:${e.readiness}%"></span></div></div><button class="outline-btn full" data-exam-open="${e.id}">${tr('details')} ${I('arrow')}</button></article>`}).join('')}</div></section>`)}
function tasksPage(){const filters=['all','high','medium','low'];return shell('tasks',`<section class="content">${pageHead(tr('tasksTitle'),state.lang==='ar'?'كل مهمة لها موعد وأولوية ومادة مرتبطة بها.':'Every task has a deadline, priority and course.','<button class="btn primary" data-action="task">'+I('plus')+tr('newTask')+'</button>')}<div class="task-toolbar"><div class="filter-tabs">${filters.map((f,i)=>`<button class="filter ${i===0?'active':''}" data-filter="${f}">${f==='all'?(state.lang==='ar'?'الكل':'All'):tr(f)}</button>`).join('')}</div><span>${state.tasks.filter(x=>!x.done).length} ${tr('remaining')}</span></div><div class="task-list" id="task-list">${state.tasks.map(taskRow).join('')}</div></section>`)}
function progressPage(){const done=state.tasks.filter(x=>x.done).length,total=state.tasks.length;const weekSessions=state.sessions.filter(s=>new Date(s.date)>=addDays(now,-6));const mins=weekSessions.reduce((a,s)=>a+s.minutes,0);return shell('progress',`<section class="content">${pageHead(tr('progressTitle'),state.lang==='ar'?'صورة واحدة عن انتظامك، حملك، واستعدادك.':'One view of consistency, workload and readiness.','<button class="btn" data-action="export">'+I('arrow')+tr('export')+'</button>')}<div class="progress-hero panel"><div><span class="eyebrow">${tr('overview')}</span><h2>${mins||state.focusMinutes} <small>${tr('minutes')}</small></h2><p>${state.lang==='ar'?'دقائق التركيز المسجلة مؤخرًا.':'Recently logged focus minutes.'}</p></div><div class="big-progress"><div class="ring-progress" style="--p:${Math.min(100,total?Math.round(done/total*100):0)}%"><span>${total?Math.round(done/total*100):0}%</span></div><small>${done}/${total} ${tr('completed')}</small></div></div><div class="stat-grid"><div class="panel stat-block"><span>${tr('weeklyLoad')}</span><strong>${state.tasks.filter(x=>!x.done).reduce((a,x)=>a+(x.minutes||50),0)}<small> min</small></strong><p>${tr('remaining')}</p></div><div class="panel stat-block"><span>${tr('readinessRisk')}</span><strong>${state.exams.filter(e=>e.readiness<70).length}</strong><p>${tr('examsSoon')}</p></div><div class="panel stat-block"><span>${tr('streak')}</span><strong>${state.streak}</strong><p>${state.lang==='ar'?'استمرارية':'consistency'}</p></div></div><div class="panel"><div class="panel-head"><div><h3>${state.lang==='ar'?'تقدم المواد':'Course progress'}</h3><p>${state.lang==='ar'?'متوسط التقدم مع الحمل المفتوح.':'Average progress with open workload.'}</p></div></div><div class="progress-table">${state.courses.map(c=>`<div class="progress-row"><div><strong>${escape(c.name)}</strong><small>${state.tasks.filter(x=>x.course==c.id&&!x.done).length} ${tr('remaining')}</small></div><div class="bar"><span style="width:${c.progress}%"></span></div><b>${c.progress}%</b></div>`).join('')}</div></div></section>`)}
function settingsPage(){return shell('settings',`<section class="content narrow">${pageHead(tr('settingsTitle'),state.lang==='ar'?'تحكم في بياناتك وتجربة مسار.':'Control your MASAR experience and profile.', '')}<div class="settings-card panel"><div class="settings-section"><div><h3>${tr('account')}</h3><p>${tr('student')}</p></div><div class="form-grid"><label><span>${tr('name')}</span><input id="set-name" value="${escape(state.name)}"></label><label><span>${tr('major')}</span><input id="set-major" value="${escape(state.major)}"></label></div></div><div class="settings-section"><div><h3>${tr('language')}</h3><p>${state.lang==='ar'?'واجهة عربية RTL أو English LTR.':'Arabic RTL or English LTR.'}</p></div><div class="segmented"><button class="${state.lang==='ar'?'active':''}" data-lang="ar">العربية</button><button class="${state.lang==='en'?'active':''}" data-lang="en">English</button></div></div><div class="settings-section"><div><h3>${tr('appearance')}</h3><p>${state.dark?tr('dark'):tr('light')}</p></div><button class="outline-btn" data-action="theme">${state.dark?I('sun'):I('moon')}${state.dark?tr('light'):tr('dark')}</button></div><div class="settings-section"><div><h3>${tr('auto')}</h3><p>${state.lang==='ar'?'عند تسجيل جلسة أو تأخير مهمة، يمكن لمسار إعادة ترتيب الخطة.':'Rebalance after logged sessions or delays.'}</p></div><button class="switch ${state.settings.autoPlan?'on':''}" data-setting="autoPlan"><span></span>${state.settings.autoPlan?tr('on'):tr('off')}</button></div><div class="settings-section"><div><h3>${tr('notify')}</h3><p>${state.lang==='ar'?'تنبيهات الامتحانات والمهام.':'Exam and task alerts.'}</p></div><button class="switch ${state.settings.notifications?'on':''}" data-setting="notifications"><span></span>${state.settings.notifications?tr('on'):tr('off')}</button></div><div class="settings-actions"><button class="btn primary" data-action="save-settings">${I('check')}${tr('save')}</button><button class="btn danger" data-action="reset">${I('trash')}${tr('reset')}</button></div></div></section>`)}
function modal(inner,wide=false){document.getElementById('modal-root').innerHTML=`<div class="modal-backdrop" data-close="1"><div class="modal ${wide?'wide':''}" data-modal-content>${inner}</div></div>`;document.querySelector('[data-modal-content]').onclick=e=>e.stopPropagation();bind()}
function formField(id,label,value='',type='text',extra=''){return `<label class="field"><span>${label}</span><input id="${id}" type="${type}" value="${escape(value)}" ${extra}></label>`}
function courseModal(id){const c=id?course(id):null;modal(`<div class="modal-head"><div><span class="eyebrow">MASAR</span><h2>${c?tr('edit'):tr('newCourse')}</h2></div><button class="icon-btn" data-close="1">${I('close')}</button></div><div class="form-grid">${formField('m-course-name',tr('title'),c?.name||'')}${formField('m-course-code',state.lang==='ar'?'الكود':'Code',c?.code||'')}${formField('m-course-teacher',tr('teacher'),c?.teacher||'')}${formField('m-course-progress',tr('progress'),c?.progress||0,'number','min="0" max="100"')}</div><label class="field"><span>${tr('difficulty')}</span><select id="m-course-difficulty"><option value="easy" ${c?.difficulty==='easy'?'selected':''}>${tr('easy')}</option><option value="medium" ${!c||c.difficulty==='medium'?'selected':''}>${tr('medium')}</option><option value="hard" ${c?.difficulty==='hard'?'selected':''}>${tr('hard')}</option></select></label><div class="modal-actions">${c?`<button class="btn danger" data-delete="course:${c.id}">${I('trash')}${tr('delete')}</button>`:''}<span></span><button class="btn" data-close="1">${tr('cancel')}</button><button class="btn primary" data-save-course="${c?.id||''}">${I('check')}${tr('save')}</button></div>`)}
function examModal(id){const e=id?state.exams.find(x=>x.id==id):null;modal(`<div class="modal-head"><div><span class="eyebrow">MASAR</span><h2>${e?tr('edit'):tr('newExam')}</h2></div><button class="icon-btn" data-close="1">${I('close')}</button></div><div class="form-grid">${formField('m-exam-title',tr('title'),e?.title||'')}${formField('m-exam-date',tr('date'),e?.date||todayIso,'date')}</div><label class="field"><span>${tr('course')}</span><select id="m-exam-course">${state.courses.map(c=>`<option value="${c.id}" ${e?.course==c.id?'selected':''}>${escape(c.name)}</option>`).join('')}</select></label>${formField('m-exam-ready',tr('readiness'),e?.readiness||50,'number','min="0" max="100"')}<div class="modal-actions">${e?`<button class="btn danger" data-delete="exam:${e.id}">${I('trash')}${tr('delete')}</button>`:''}<span></span><button class="btn" data-close="1">${tr('cancel')}</button><button class="btn primary" data-save-exam="${e?.id||''}">${I('check')}${tr('save')}</button></div>`)}
function taskModal(id){const x=id?state.tasks.find(t=>t.id==id):null;modal(`<div class="modal-head"><div><span class="eyebrow">MASAR</span><h2>${x?tr('edit'):tr('newTask')}</h2></div><button class="icon-btn" data-close="1">${I('close')}</button></div>${formField('m-task-title',tr('title'),x?.title||'')}<div class="form-grid">${formField('m-task-date',tr('date'),x?.due||todayIso,'date')}${formField('m-task-minutes',state.lang==='ar'?'المدة':'Minutes',x?.minutes||50,'number','min="10" max="240"')}</div><label class="field"><span>${tr('course')}</span><select id="m-task-course">${state.courses.map(c=>`<option value="${c.id}" ${x?.course==c.id?'selected':''}>${escape(c.name)}</option>`).join('')}</select></label><label class="field"><span>${tr('priority')}</span><select id="m-task-priority"><option value="high" ${x?.priority==='high'?'selected':''}>${tr('high')}</option><option value="medium" ${!x||x.priority==='medium'?'selected':''}>${tr('medium')}</option><option value="low" ${x?.priority==='low'?'selected':''}>${tr('low')}</option></select></label><div class="modal-actions">${x?`<button class="btn danger" data-delete="task:${x.id}">${I('trash')}${tr('delete')}</button>`:''}<span></span><button class="btn" data-close="1">${tr('cancel')}</button><button class="btn primary" data-save-task="${x?.id||''}">${I('check')}${tr('save')}</button></div>`)}
function searchModal(){const all=[...state.courses.map(c=>({title:c.name,meta:c.code,kind:tr('courses'),action:`course:${c.id}`})),...state.exams.map(e=>({title:e.title,meta:fmtDate(e.date),kind:tr('exams'),action:`exam:${e.id}`})),...state.tasks.map(x=>({title:x.title,meta:courseName(x.course),kind:tr('tasks'),action:`task:${x.id}`}))];modal(`<div class="modal-head"><div><span class="eyebrow">MASAR</span><h2>${state.lang==='ar'?'بحث سريع':'Quick search'}</h2><p>${tr('searchHint')}</p></div><button class="icon-btn" data-close="1">${I('close')}</button></div><div class="search-box">${I('search')}<input id="global-search" autofocus placeholder="${escape(tr('search'))}"><kbd>ESC</kbd></div><div id="search-results">${searchResults(all,'')}</div>`);const input=document.getElementById('global-search');input?.focus();input?.addEventListener('input',()=>document.getElementById('search-results').innerHTML=searchResults(all,input.value))}
function searchResults(all,q){const z=q?all.filter(a=>(a.title+' '+a.meta).toLowerCase().includes(q.toLowerCase())):all.slice(0,8);return z.length?z.map(a=>`<button class="search-result" data-open="${a.action}"><div class="list-icon">${I(a.action.startsWith('course')?'book':a.action.startsWith('exam')?'exam':'tasks')}</div><div><strong>${escape(a.title)}</strong><small>${escape(a.kind)} · ${escape(a.meta)}</small></div>${I('arrow')}</button>`).join(''):`<div class="empty-state">${tr('noResults')}</div>`}
function notifications(){const alerts=state.exams.filter(e=>daysLeft(e.date)<=7&&e.readiness<70).map(e=>({title:e.title,meta:`${Math.max(0,daysLeft(e.date))} ${tr('daysTo')}`}));modal(`<div class="modal-head"><div><span class="eyebrow">MASAR</span><h2>${tr('notifications')}</h2></div><button class="icon-btn" data-close="1">${I('close')}</button></div>${alerts.length?alerts.map(a=>`<div class="notice"><div class="notice-icon">${I('exam')}</div><div><strong>${escape(a.title)}</strong><small>${escape(a.meta)}</small></div></div>`).join(''):`<div class="empty-state">${I('check')}<div><strong>${tr('noNotifications')}</strong></div></div>`}`)}
function analysisModal(){modal(`<div class="modal-head"><div><span class="eyebrow">MASAR</span><h2>${tr('analysis')}</h2><p>${state.lang==='ar'?'شفافية القرار: تعرف لماذا هذه المهمة قبل غيرها.':'Decision transparency: know why this task comes first.'}</p></div><button class="icon-btn" data-close="1">${I('close')}</button></div>${analysisPoints()}<div class="modal-actions"><span></span><button class="btn primary" data-close="1">${tr('close')}</button></div>`,true)}
function recoveryModal(){modal(`<div class="modal-head"><div><span class="eyebrow">${tr('recovery')}</span><h2>${tr('recoveryTitle')}</h2><p>${tr('recoveryText')}</p></div><button class="icon-btn" data-close="1">${I('close')}</button></div><div class="strategy-list"><label class="strategy active"><input type="radio" name="recovery" value="move" checked><div><strong>${tr('move')}</strong><small>${state.lang==='ar'?'يُعيد توزيع المهام المفتوحة على الأيام القادمة حسب سعتها.':'Redistribute open tasks across future capacity.'}</small></div></label><label class="strategy"><input type="radio" name="recovery" value="defer"><div><strong>${tr('defer')}</strong><small>${state.lang==='ar'?'يؤخر المهام منخفضة الأولوية ويحمي الامتحانات.':'Push low-priority tasks while protecting exams.'}</small></div></label><label class="strategy"><input type="radio" name="recovery" value="extend"><div><strong>${tr('extend')}</strong><small>${state.lang==='ar'?'يضيف يومًا للخطة بدل ضغط ساعات طويلة في يوم واحد.':'Add capacity rather than stacking long days.'}</small></div></label></div><div class="modal-actions"><span></span><button class="btn" data-close="1">${tr('cancel')}</button><button class="btn primary" data-apply-recovery>${tr('apply')}</button></div>`,true)}
function focusStart(){const task=priorityTask();if(!task)return toast(tr('empty'));let seconds=(task.minutes||50)*60,paused=false,finished=false;const root=document.getElementById('focus-root');root.innerHTML=`<div class="focus-overlay"><div class="focus-panel"><div class="focus-top"><span>MASAR FOCUS</span><button class="icon-btn" id="focus-exit">${I('close')}</button></div><div class="focus-course">${escape(courseName(task.course))}</div><h2>${escape(task.title)}</h2><div class="focus-timer" id="focus-timer">${clock(seconds)}</div><p>${state.lang==='ar'?'جلسة واحدة. مهمة واحدة.':'One session. One task.'}</p><div class="focus-progress"><span style="width:0%" id="focus-progress"></span></div><div class="focus-actions"><button class="btn" id="focus-toggle">${I('play')}${tr('pause')}</button><button class="btn primary large" id="focus-finish">${I('check')}${tr('finish')}</button></div></div></div>`;const timer=document.getElementById('focus-timer'),bar=document.getElementById('focus-progress'),toggle=document.getElementById('focus-toggle');const total=seconds;const interval=setInterval(()=>{if(paused)return;seconds--;timer.textContent=clock(seconds);bar.style.width=Math.min(100,((total-seconds)/total)*100)+'%';if(seconds<=0)finish()},1000);function finish(){if(finished)return;finished=true;clearInterval(interval);task.done=true;state.focusMinutes+=(task.minutes||50);state.sessions.push({id:Date.now(),taskId:task.id,minutes:task.minutes||50,date:todayIso});state.streak=Math.max(state.streak,state.streak+1);save();root.innerHTML='';toast(tr('sessionLogged'));render()}toggle.onclick=()=>{paused=!paused;toggle.innerHTML=paused?I('play')+tr('resume'):I('play')+tr('pause')};document.getElementById('focus-finish').onclick=finish;document.getElementById('focus-exit').onclick=()=>{if(confirm(state.lang==='ar'?'الخروج الآن؟ لن يتم تسجيل هذه الجلسة.':'Exit now? This session will not be logged.')){clearInterval(interval);root.innerHTML=''}} }
const clock=s=>`${String(Math.floor(Math.max(0,s)/60)).padStart(2,'0')}:${String(Math.max(0,s)%60).padStart(2,'0')}`;
function rebuild(shouldRender=true){
  const open=state.tasks.filter(x=>!x.done);
  const sorted=open.slice().sort((a,b)=>{
    const score=x=>({high:30,medium:15,low:5}[x.priority]||5)+(state.exams.find(e=>e.course==x.course)?Math.max(0,20-daysLeft(state.exams.find(e=>e.course==x.course).date)):0)+(100-(course(x.course)?.progress||0))/10;
    return score(b)-score(a);
  });
  state.plan=[];
  sorted.forEach((x,i)=>state.plan.push({taskId:x.id,title:x.title,course:x.course,date:iso(addDays(now,Math.floor(i/3))),slot:(i%3)+1,priority:x.priority,minutes:x.minutes||50}));
  save();
  if(shouldRender){toast(tr('planUpdated'));render();}
}

function applyRecovery(){
  const strategy=document.querySelector('input[name="recovery"]:checked')?.value||'move';
  const open=state.tasks.filter(x=>!x.done);
  if(!open.length){
    document.getElementById('modal-root').innerHTML='';
    toast(state.lang==='ar'?'لا توجد مهام مفتوحة لإعادة توزيعها.':'There are no open tasks to rebalance.');
    return;
  }
  if(strategy==='move'){
    // Rebuild into future slots while preserving task deadlines as much as possible.
    const sorted=open.slice().sort((a,b)=>{
      const pa={high:3,medium:2,low:1}[a.priority]||1, pb={high:3,medium:2,low:1}[b.priority]||1;
      return pb-pa || daysLeft(a.due)-daysLeft(b.due);
    });
    state.plan=[];
    sorted.forEach((x,i)=>state.plan.push({taskId:x.id,title:x.title,course:x.course,date:iso(addDays(now,Math.floor(i/3))),slot:(i%3)+1,priority:x.priority,minutes:x.minutes||50}));
  }else if(strategy==='defer'){
    state.tasks.filter(x=>!x.done&&x.priority==='low').forEach(x=>x.due=iso(addDays(new Date(x.due+'T12:00:00'),2)));
    rebuild(false);
  }else if(strategy==='extend'){
    const tail=open.slice(-Math.min(2,open.length));
    tail.forEach(x=>x.due=iso(addDays(new Date(x.due+'T12:00:00'),1)));
    rebuild(false);
  }
  save();
  document.getElementById('modal-root').innerHTML='';
  toast(tr('planUpdated'));
  render();
}

function saveCourse(id){const name=document.getElementById('m-course-name').value.trim();if(!name)return toast(tr('required'));const data={code:document.getElementById('m-course-code').value.trim()||'NEW',name,teacher:document.getElementById('m-course-teacher').value.trim()||'—',progress:Math.max(0,Math.min(100,Number(document.getElementById('m-course-progress').value)||0)),difficulty:document.getElementById('m-course-difficulty').value};if(id){Object.assign(course(id),data)}else state.courses.push({id:Date.now(),...data});save();document.getElementById('modal-root').innerHTML='';toast(tr('save'));render()}
function saveExam(id){
  const title=document.getElementById('m-exam-title')?.value.trim();
  const date=document.getElementById('m-exam-date')?.value;
  const courseEl=document.getElementById('m-exam-course');
  const readyEl=document.getElementById('m-exam-ready');
  if(!title||!date){toast(tr('required'));return false;}
  if(!courseEl||!courseEl.value){toast(state.lang==='ar'?'اختر المادة أولًا.':'Choose a course first.');return false;}
  const courseId=Number(courseEl.value);
  const readiness=Math.max(0,Math.min(100,Number(readyEl?.value)||0));
  const data={title,date,course:courseId,readiness};
  if(id){const target=state.exams.find(e=>e.id==id);if(target)Object.assign(target,data);}
  else state.exams.push({id:Date.now(),...data});
  save();
  document.getElementById('modal-root').innerHTML='';
  toast(state.lang==='ar'?'تم حفظ الامتحان بنجاح.':'Exam saved successfully.');
  render();
  return true;
}

function saveTask(id){const title=document.getElementById('m-task-title').value.trim(),due=document.getElementById('m-task-date').value;if(!title||!due)return toast(tr('required'));const data={title,due,course:Number(document.getElementById('m-task-course').value),priority:document.getElementById('m-task-priority').value,minutes:Math.max(10,Number(document.getElementById('m-task-minutes').value)||50)};if(id)Object.assign(state.tasks.find(x=>x.id==id),data);else state.tasks.push({id:Date.now(),...data,done:false});save();document.getElementById('modal-root').innerHTML='';if(state.settings.autoPlan)rebuild();else render();toast(tr('save'))}
function deleteItem(type,id){if(!confirm(tr('confirmDelete')))return;if(type==='course')state.courses=state.courses.filter(x=>x.id!=id);if(type==='exam')state.exams=state.exams.filter(x=>x.id!=id);if(type==='task')state.tasks=state.tasks.filter(x=>x.id!=id);save();document.getElementById('modal-root').innerHTML='';toast(tr('deleted'));render()}
function action(a){if(a==='theme'){state.dark=!state.dark;save();render()}else if(a==='search')searchModal();else if(a==='notifications')notifications();else if(a==='course')courseModal();else if(a==='exam')examModal();else if(a==='task')taskModal();else if(a==='focus')focusStart();else if(a==='recovery')recoveryModal();else if(a==='analysis')analysisModal();else if(a==='rebuild')rebuild();else if(a==='save-settings'){state.name=document.getElementById('set-name').value.trim()||state.name;state.major=document.getElementById('set-major').value.trim()||state.major;save();toast(tr('save'));render()}else if(a==='export'){const txt=`MASAR — ${new Date().toLocaleString()}\n\nCourses\n${state.courses.map(c=>`${c.name}: ${c.progress}%`).join('\n')}\n\nTasks remaining: ${state.tasks.filter(x=>!x.done).length}\nFocus minutes: ${state.focusMinutes}`;const blob=new Blob([txt],{type:'text/plain;charset=utf-8'}),url=URL.createObjectURL(blob),a=document.createElement('a');a.href=url;a.download='masar-progress.txt';a.click();URL.revokeObjectURL(url)}else if(a==='reset'){localStorage.removeItem(KEY);state=load();toast(tr('save'));render()}}
function bind(){
  document.querySelectorAll('[data-nav]').forEach(el=>el.onclick=()=>{location.hash=el.dataset.nav;render()});
  document.querySelectorAll('[data-action]').forEach(el=>el.onclick=()=>action(el.dataset.action));
  document.querySelectorAll('[data-close]').forEach(el=>el.onclick=()=>document.getElementById('modal-root').innerHTML='');
  document.querySelectorAll('[data-task-toggle]').forEach(el=>el.onclick=()=>{const x=state.tasks.find(t=>t.id==el.dataset.taskToggle);if(x){x.done=!x.done;save();if(state.settings.autoPlan)rebuild();else render()}});
  document.querySelectorAll('[data-setting]').forEach(el=>el.onclick=()=>{state.settings[el.dataset.setting]=!state.settings[el.dataset.setting];save();render()});
  document.querySelectorAll('[data-lang]').forEach(el=>el.onclick=()=>setLang(el.dataset.lang));
  document.querySelectorAll('[data-course-open]').forEach(el=>el.onclick=()=>courseModal(Number(el.dataset.courseOpen)));
  document.querySelectorAll('[data-exam-open]').forEach(el=>el.onclick=()=>examModal(Number(el.dataset.examOpen)));
  document.querySelectorAll('[data-exam]').forEach(el=>el.onclick=e=>{if(!e.target.closest('button'))examModal(Number(el.dataset.exam))});
  document.querySelectorAll('[data-menu]').forEach(el=>el.onclick=e=>{e.stopPropagation();const [type,id]=el.dataset.menu.split(':');if(type==='course')courseModal(Number(id));if(type==='exam')examModal(Number(id));if(type==='task')taskModal(Number(id))});
  document.querySelectorAll('[data-save-course]').forEach(el=>el.onclick=()=>saveCourse(el.dataset.saveCourse?Number(el.dataset.saveCourse):null));
  document.querySelectorAll('[data-save-exam]').forEach(el=>el.onclick=()=>saveExam(el.dataset.saveExam?Number(el.dataset.saveExam):null));
  document.querySelectorAll('[data-save-task]').forEach(el=>el.onclick=()=>saveTask(el.dataset.saveTask?Number(el.dataset.saveTask):null));
  document.querySelectorAll('[data-delete]').forEach(el=>el.onclick=()=>{const [type,id]=el.dataset.delete.split(':');deleteItem(type,Number(id))});
  document.querySelectorAll('[data-apply-recovery]').forEach(el=>el.onclick=applyRecovery);
  document.querySelectorAll('[data-open]').forEach(el=>el.onclick=()=>{const [type,id]=el.dataset.open.split(':');document.getElementById('modal-root').innerHTML='';if(type==='course')courseModal(Number(id));if(type==='exam')examModal(Number(id));if(type==='task')taskModal(Number(id))});
  document.querySelectorAll('[data-filter]').forEach(el=>el.onclick=()=>{document.querySelectorAll('.filter').forEach(x=>x.classList.remove('active'));el.classList.add('active');const f=el.dataset.filter;document.querySelectorAll('#task-list .task-row').forEach(row=>{const b=row.querySelector('[data-task-toggle]');const id=b?.dataset.taskToggle;const x=state.tasks.find(t=>t.id==id);row.style.display=f==='all'||(x&&!x.done&&x.priority===f)?'flex':'none'})});
  document.querySelectorAll('.strategy input').forEach(r=>r.onchange=()=>document.querySelectorAll('.strategy').forEach(s=>s.classList.toggle('active',s.querySelector('input').checked)));
  document.querySelectorAll('.modal input').forEach(input=>input.addEventListener('keydown',e=>{if(e.key==='Enter'){e.preventDefault();const btn=input.closest('.modal')?.querySelector('[data-save-exam],[data-save-course],[data-save-task],[data-apply-recovery]');if(btn)btn.click()}}));
}

function seedPlan(){if(state.plan.length)return;const open=state.tasks.filter(x=>!x.done).slice().sort((a,b)=>({high:3,medium:2,low:1}[b.priority]-({high:3,medium:2,low:1}[a.priority])));state.plan=open.map((x,i)=>({taskId:x.id,title:x.title,course:x.course,date:iso(addDays(now,Math.floor(i/3))),slot:(i%3)+1,priority:x.priority,minutes:x.minutes||50}));save()}
function render(){document.documentElement.lang=state.lang;document.documentElement.dir=state.lang==='ar'?'rtl':'ltr';document.body.classList.toggle('dark',state.dark);const page=location.hash.replace('#','')||'today';const map={today:todayPage,planner:plannerPage,courses:coursesPage,exams:examsPage,tasks:tasksPage,progress:progressPage,settings:settingsPage};document.getElementById('app').innerHTML=(map[page]||todayPage)();bind()}
window.addEventListener('hashchange',render);window.addEventListener('keydown',e=>{if((e.ctrlKey||e.metaKey)&&e.key.toLowerCase()==='k'){e.preventDefault();searchModal()}if(e.key==='Escape'){document.getElementById('modal-root').innerHTML='';document.getElementById('focus-root').innerHTML=''}if(e.key.toLowerCase()==='f'&&!['INPUT','TEXTAREA','SELECT'].includes(document.activeElement?.tagName)){e.preventDefault();focusStart()}});
seedPlan();
render();
})();
