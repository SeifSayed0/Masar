'use strict';
const KEY='masar.v4.state';
const today=new Date(); const iso=d=>new Date(d).toISOString().slice(0,10); const addDays=(d,n)=>{const x=new Date(d);x.setDate(x.getDate()+n);return iso(x)};
const uid=()=>crypto.randomUUID?crypto.randomUUID():Date.now().toString(36)+Math.random().toString(36).slice(2);
const icons={home:'<path d="m3 10 9-7 9 7v10a1 1 0 0 1-1 1h-5v-6H9v6H4a1 1 0 0 1-1-1z"/>',calendar:'<rect x="3" y="4" width="18" height="17" rx="2"/><path d="M16 2v4M8 2v4M3 10h18"/>',book:'<path d="M4 5.5A2.5 2.5 0 0 1 6.5 3H20v17H6.5A2.5 2.5 0 0 0 4 22z"/><path d="M4 5.5V19a3 3 0 0 1 3-3h13"/>',exam:'<rect x="4" y="3" width="16" height="18" rx="2"/><path d="m8 9 2 2 4-4M8 15h8"/>',task:'<rect x="4" y="4" width="16" height="16" rx="2"/><path d="m8 12 2.5 2.5L16 9"/>',chart:'<path d="M4 19V5M4 19h17"/><path d="m7 15 4-5 3 3 5-7"/>',focus:'<circle cx="12" cy="12" r="8"/><path d="M12 8v5l3 2M12 2v2M22 12h-2"/>',settings:'<path d="M12 15.5a3.5 3.5 0 1 0 0-7 3.5 3.5 0 0 0 0 7Z"/><path d="M19.4 15a1.7 1.7 0 0 0 .34 1.88l.06.06-1.7 1.7-.06-.06a1.7 1.7 0 0 0-1.88-.34 1.7 1.7 0 0 0-1.03 1.56V20h-2.4v-.2a1.7 1.7 0 0 0-1.03-1.56 1.7 1.7 0 0 0-1.88.34l-.06.06-1.7-1.7.06-.06A1.7 1.7 0 0 0 8.4 15a1.7 1.7 0 0 0-1.56-1.03H6v-2.4h.84A1.7 1.7 0 0 0 8.4 10a1.7 1.7 0 0 0-.34-1.88L8 8.06l1.7-1.7.06.06A1.7 1.7 0 0 0 11.64 6.4 1.7 1.7 0 0 0 12.67 4.84V4h2.4v.84A1.7 1.7 0 0 0 16.1 6.4a1.7 1.7 0 0 0 1.88-.34l.06-.06 1.7 1.7-.06.06A1.7 1.7 0 0 0 19.4 10a1.7 1.7 0 0 0 1.56 1.03h.84v2.4h-.84A1.7 1.7 0 0 0 19.4 15Z"/>',search:'<circle cx="11" cy="11" r="7"/><path d="m20 20-4-4"/>',bell:'<path d="M18 8a6 6 0 0 0-12 0c0 7-3 7-3 9h18c0-2-3-2-3-9M10 21h4"/>',plus:'<path d="M12 5v14M5 12h14"/>',play:'<path d="m9 6 10 6-10 6z"/>',pause:'<path d="M8 5v14M16 5v14"/>',check:'<path d="m5 12 4 4L19 6"/>',close:'<path d="m6 6 12 12M18 6 6 18"/>',refresh:'<path d="M20 11a8 8 0 1 0 1 4"/><path d="M20 5v6h-6"/>',zap:'<path d="m13 2-9 12h7l-1 8 9-12h-7z"/>',moon:'<path d="M20 15.5A8 8 0 0 1 8.5 4 8 8 0 1 0 20 15.5Z"/>',sun:'<circle cx="12" cy="12" r="4"/><path d="M12 2v2M12 20v2M4.93 4.93l1.41 1.41M17.66 17.66l1.41 1.41M2 12h2M20 12h2M4.93 19.07l1.41-1.41M17.66 6.34l1.41-1.41"/>',more:'<circle cx="5" cy="12" r="1"/><circle cx="12" cy="12" r="1"/><circle cx="19" cy="12" r="1"/>',download:'<path d="M12 3v12m0 0 4-4m-4 4-4-4M5 21h14"/>',arrow:'<path d="m9 18 6-6-6-6"/>'};
const I=(name,size=18)=>`<svg width="${size}" height="${size}" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true">${icons[name]||icons.more}</svg>`;
const T={ar:{today:'اليوم',planner:'المخطط',courses:'المواد',exams:'الامتحانات',tasks:'المهام',progress:'التقدم',focus:'التركيز',settings:'الإعدادات',search:'ابحث في مسار',addCourse:'إضافة مادة',addExam:'إضافة امتحان',addTask:'إضافة مهمة',rebuild:'إعادة بناء الخطة',analysis:'تحليل الخطة',recover:'اتأخرت عن الخطة؟',start:'ابدأ الجلسة المقترحة',whatNow:'ماذا تذاكر الآن؟',schedule:'خطة اليوم',health:'صحة الخطة',week:'هذا الأسبوع',save:'حفظ',cancel:'إلغاء',apply:'تطبيق',edit:'تعديل',delete:'حذف',close:'إغلاق',done:'تم',pause:'إيقاف مؤقت',resume:'استكمال',finish:'إنهاء الجلسة',noData:'لا توجد بيانات بعد',name:'الاسم',date:'التاريخ',course:'المادة',readiness:'الجاهزية',priority:'الأولوية',minutes:'الدقائق',title:'العنوان',status:'الحالة',high:'عالية',medium:'متوسطة',low:'منخفضة',settingsTitle:'تفضيلات مسار',language:'اللغة',theme:'المظهر',adaptive:'التكييف التلقائي',adaptiveDesc:'أعد ترتيب الخطة عندما تتغير قدرتك أو تتأخر عن جلسة.',notifications:'التنبيهات',notificationsDesc:'تنبيهات الامتحانات والجلسات القادمة.',dark:'داكن',light:'فاتح',arabic:'العربية',english:'English',student:'طالب',weekLoad:'حمل الأسبوع',completed:'مكتمل',remaining:'متبقي',days:'أيام',sessions:'جلسات',minutesShort:'د',examSoon:'امتحان قريب',risk:'المخاطرة',recoveryTitle:'اتأخرت عن الخطة؟',recoveryDesc:'مسار يعيد ترتيب الوقت حول الامتحانات والأولويات بدل تكديس كل شيء في يوم واحد.',redistribute:'إعادة توزيع الجلسات',redistributeDesc:'يُعيد توزيع المهام المفتوحة على الأيام القادمة حسب سعتها.',defer:'تأجيل الأقل أولوية',deferDesc:'يؤخر المهام منخفضة الأولوية ويحمي الامتحانات.',extend:'فتح مساحة زمنية إضافية',extendDesc:'يضيف يومًا للخطة بدل ضغط ساعات طويلة في يوم واحد.',planUpdated:'تم تحديث الخطة',saved:'تم الحفظ',deleted:'تم الحذف',invalid:'راجع البيانات المدخلة',startSession:'بدء جلسة',noTasks:'لا توجد مهام مفتوحة',noExams:'لا توجد امتحانات',noCourses:'لا توجد مواد',ready:'جاهزية',progress:'التقدم',focusTitle:'وضع التركيز',focusDesc:'جلسة واحدة. هدف واحد. بدون تشتيت.',minutesDone:'دقائق تركيز',sessionsDone:'جلسات منجزة',back:'رجوع',confirmDelete:'هل تريد حذف هذا العنصر؟',searchEmpty:'لا توجد نتائج مطابقة',command:'الأوامر السريعة',next:'التالي',all:'الكل'},en:{today:'Today',planner:'Planner',courses:'Courses',exams:'Exams',tasks:'Tasks',progress:'Progress',focus:'Focus',settings:'Settings',search:'Search Masar',addCourse:'Add course',addExam:'Add exam',addTask:'Add task',rebuild:'Rebuild plan',analysis:'Plan analysis',recover:'Fell behind?',start:'Start suggested session',whatNow:'What should you study now?',schedule:'Today',health:'Plan health',week:'This week',save:'Save',cancel:'Cancel',apply:'Apply',edit:'Edit',delete:'Delete',close:'Close',done:'Done',pause:'Pause',resume:'Resume',finish:'Finish session',noData:'No data yet',name:'Name',date:'Date',course:'Course',readiness:'Readiness',priority:'Priority',minutes:'Minutes',title:'Title',status:'Status',high:'High',medium:'Medium',low:'Low',settingsTitle:'Masar preferences',language:'Language',theme:'Theme',adaptive:'Adaptive planning',adaptiveDesc:'Rebalance your plan when capacity changes or sessions are missed.',notifications:'Notifications',notificationsDesc:'Exam and upcoming-session alerts.',dark:'Dark',light:'Light',arabic:'العربية',english:'English',student:'Student',weekLoad:'Week load',completed:'Completed',remaining:'Remaining',days:'days',sessions:'sessions',minutesShort:'m',examSoon:'Exam soon',risk:'Risk',recoveryTitle:'Fell behind?',recoveryDesc:'Masar reshapes time around exams and priorities instead of stacking everything into one day.',redistribute:'Redistribute sessions',redistributeDesc:'Spread open work across the next days based on capacity.',defer:'Defer low priority',deferDesc:'Push lower-priority work back and protect exams.',extend:'Open extra time',extendDesc:'Add a day to the plan instead of cramming long hours.',planUpdated:'Plan updated',saved:'Saved',deleted:'Deleted',invalid:'Check the entered data',startSession:'Start session',noTasks:'No open tasks',noExams:'No exams',noCourses:'No courses',ready:'Readiness',progress:'Progress',focusTitle:'Focus mode',focusDesc:'One session. One goal. No noise.',minutesDone:'Focus minutes',sessionsDone:'Sessions completed',back:'Back',confirmDelete:'Delete this item?',searchEmpty:'No matching results',command:'Quick actions',next:'Next',all:'All'}};
const demo={lang:'ar',theme:'light',student:{name:'سيف',major:'وقاية النبات'},settings:{adaptive:true,notifications:true},courses:[{id:'c1',name:'الإحصاء الحيوي',code:'BIO-STAT',progress:42,color:'blue'},{id:'c2',name:'أمراض النبات',code:'PLP-204',progress:68,color:'green'},{id:'c3',name:'تصنيف نباتي',code:'TAX-210',progress:31,color:'amber'}],exams:[{id:'e1',title:'امتحان الإحصاء الحيوي',courseId:'c1',date:addDays(today,4),readiness:42},{id:'e2',title:'عملي أمراض النبات',courseId:'c2',date:addDays(today,11),readiness:68}],tasks:[{id:'t1',title:'حل 20 سؤالًا من الإحصاء الحيوي',courseId:'c1',date:iso(today),minutes:50,priority:'high',done:false},{id:'t2',title:'مراجعة محاضرة أمراض النبات',courseId:'c2',date:iso(today),minutes:40,priority:'medium',done:false},{id:'t3',title:'بطاقات تصنيف العائلات',courseId:'c3',date:addDays(today,1),minutes:30,priority:'medium',done:false},{id:'t4',title:'تلخيص الفصل الثاني',courseId:'c1',date:addDays(today,2),minutes:35,priority:'low',done:false}],sessions:[],planVersion:1,createdAt:new Date().toISOString()};
let state=load(); let route='today'; let focus={taskId:null,remaining:0,total:0,timer:null,running:false};
function load(){try{const s=JSON.parse(localStorage.getItem(KEY));return s&&s.courses?s:structuredClone(demo)}catch{return structuredClone(demo)}}
function save(){localStorage.setItem(KEY,JSON.stringify(state));window.dispatchEvent(new CustomEvent('masar:state-change'))}
function t(k){return T[state.lang]?.[k]||T.ar[k]||k}
function course(id){return state.courses.find(x=>x.id===id)}
function fmtDate(d){return new Intl.DateTimeFormat(state.lang==='ar'?'ar-EG':'en-US',{day:'2-digit',month:'short'}).format(new Date(d))}
function daysLeft(d){const a=new Date();a.setHours(0,0,0,0);const b=new Date(d);b.setHours(0,0,0,0);return Math.ceil((b-a)/86400000)}
function esc(s=''){return String(s).replace(/[&<>"']/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#039;'}[m]))}
function toast(msg){const root=document.getElementById('toast-root');root.innerHTML=`<div class="toast">${esc(msg)}</div>`;setTimeout(()=>root.innerHTML='',2600)}
function pageHead(title,desc,actions=''){return `<div class="section-head"><div><h1 class="section-title" style="font-size:25px;margin:0">${esc(title)}</h1><p>${esc(desc)}</p></div><div class="toolbar-actions">${actions}</div></div>`}
function btn(action,label,icon=''){return `<button class="btn ${action.startsWith('primary')?'btn-primary':''}" data-action="${action}">${icon?I(icon,16):''}${esc(label)}</button>`}
function nav(){const items=[['today','today','home'],['planner','planner','calendar'],['courses','courses','book'],['exams','exams','exam'],['tasks','tasks','task'],['progress','progress','chart'],['focus','focus','focus']];return `<nav class="nav"><div class="nav-label">${state.lang==='ar'?'مساحتك الدراسية':'Workspace'}</div>${items.map(([r,k,i])=>`<button class="nav-btn ${route===r?'active':''}" data-route="${r}"><span class="nav-icon">${I(i,18)}</span><span>${t(k)}</span></button>`).join('')}</nav>`}
function shell(content){document.documentElement.lang=state.lang;document.documentElement.dir=state.lang==='ar'?'rtl':'ltr';document.body.classList.toggle('dark',state.theme==='dark');document.body.classList.toggle('rtl',state.lang==='ar');document.getElementById('app').innerHTML=`<div class="app-shell"><aside class="sidebar"><div class="brand"><div class="brand-mark">م</div><div><div class="brand-name">مسار</div><div class="brand-sub">Student Operating System</div></div></div>${nav()}<div class="sidebar-bottom"><div class="streak"><div class="streak-dot">${I('zap',16)}</div><div><strong>${Math.max(1,state.sessions.length+1)} ${state.lang==='ar'?'يوم':'day'} ${state.lang==='ar'?'متتابع':'streak'}</strong><span>${state.lang==='ar'?'استمر على المسار':'Keep the momentum'}</span></div></div><div class="user-chip"><div class="avatar">${esc((state.student.name||'س').slice(0,1))}</div><div class="user-meta"><strong>${esc(state.student.name)}</strong><span>${esc(state.student.major)}</span></div><button class="icon-btn" data-route="settings" aria-label="${t('settings')}">${I('settings',16)}</button></div></div></aside><main class="main"><header class="topbar"><div class="top-left"><div><div class="eyebrow">${route==='today'?(state.lang==='ar'?'الثلاثاء، 8 سبتمبر 2026':'Tuesday, Sep 8, 2026'):t(route)}</div><div class="page-title">${route==='today'?'مسار':t(route)}</div></div></div><div class="top-right"><button class="account-btn" data-action="account"><span class="avatar mini">${esc((state.student.name||'س').slice(0,1))}</span><span class="account-name">${esc(state.student.name||t('student'))}</span></button><button class="search" data-action="search" aria-label="${t('search')}">${I('search',16)}<span>${t('search')}</span><kbd>⌘ K</kbd></button><button class="icon-btn" data-action="notifications" aria-label="${t('notifications')}">${I('bell',17)}</button><button class="icon-btn" data-action="theme" aria-label="${t('theme')}">${I(state.theme==='dark'?'sun':'moon',17)}</button></div></header><section class="content">${content}</section></main><nav class="mobile-nav">${[['today','home'],['planner','calendar'],['courses','book'],['tasks','task'],['focus','focus']].map(([r,i])=>`<button class="nav-btn ${route===r?'active':''}" data-route="${r}"><span class="nav-icon">${I(i,17)}</span><span>${t(r)}</span></button>`).join('')}</nav></div>`}
function render(){if(route==='today')return renderToday();if(route==='planner')return renderPlanner();if(route==='courses')return renderCourses();if(route==='exams')return renderExams();if(route==='tasks')return renderTasks();if(route==='progress')return renderProgress();if(route==='focus')return renderFocus();if(route==='settings')return renderSettings()}
function renderToday(){const open=state.tasks.filter(x=>!x.done);const next=open.slice().sort((a,b)=>score(b)-score(a))[0]||state.tasks[0];const ex=state.exams.slice().sort((a,b)=>new Date(a.date)-new Date(b.date))[0];const dleft=ex?daysLeft(ex.date):0;const totalMin=state.tasks.reduce((s,x)=>s+(x.done?0:x.minutes||0),0);const weekDone=state.sessions.reduce((s,x)=>s+(x.minutes||0),0);const avg=state.courses.length?Math.round(state.courses.reduce((s,x)=>s+x.progress,0)/state.courses.length):0;const sessions=open.slice(0,4);shell(`<section class="hero"><div class="hero-copy"><div class="hero-kicker"><i class="live"></i>${state.lang==='ar'?'قرار اليوم':'Today’s decision'}</div><h1>${t('whatNow')}</h1><p>${next?`${esc(next.title)} — ${course(next.courseId)?.name||''}. ${ex?`${esc(ex.title)} ${state.lang==='ar'?'بعد':'in'} ${dleft} ${t('days')}.`:''}`:t('noTasks')}</p><div class="hero-actions">${next?btn('primary-start',t('start'),'play'):''}${btn('recover',t('recover'),'refresh')}</div></div><div class="hero-stats"><div class="mini-stat"><span>${t('weekLoad')}</span><strong>${totalMin} ${t('minutesShort')}</strong></div><div class="mini-stat"><span>${t('ready')}</span><strong>${avg}%</strong></div><div class="mini-stat"><span>${t('sessionsDone')}</span><strong>${state.sessions.length}</strong></div><div class="mini-stat"><span>${ex?t('examSoon'):t('next')}</span><strong>${ex?dleft+' '+t('days'):'—'}</strong></div></div></section><section class="section"><div class="grid-4"><div class="card stat-card"><div class="stat-top"><span>${t('completed')}</span><span class="icon-tile">${I('check',16)}</span></div><div class="stat-value">${state.tasks.filter(x=>x.done).length}</div><div class="stat-note">${state.tasks.length} ${state.lang==='ar'?'مهمة إجمالًا':'tasks total'}</div></div><div class="card stat-card"><div class="stat-top"><span>${t('minutesDone')}</span><span class="icon-tile">${I('focus',16)}</span></div><div class="stat-value">${weekDone}</div><div class="stat-note">${state.lang==='ar'?'هذا السجل محليًا':'Saved locally'}</div></div><div class="card stat-card"><div class="stat-top"><span>${t('exams')}</span><span class="icon-tile">${I('exam',16)}</span></div><div class="stat-value">${state.exams.length}</div><div class="stat-note">${state.exams.filter(e=>daysLeft(e.date)<=7).length} ${state.lang==='ar'?'خلال 7 أيام':'within 7 days'}</div></div><div class="card stat-card"><div class="stat-top"><span>${t('courses')}</span><span class="icon-tile">${I('book',16)}</span></div><div class="stat-value">${state.courses.length}</div><div class="stat-note">${state.courses.filter(c=>c.progress>=70).length} ${state.lang==='ar'?'قريبة من الجاهزية':'near ready'}</div></div></div></section><section class="section"><div class="grid-2"><div class="card list-card"><div class="section-head"><div><h2>${t('schedule')}</h2><p>${state.lang==='ar'?'أقل عدد من القرارات، أهمها أولًا.':'Fewer decisions, highest value first.'}</p></div>${btn('route-planner',t('planner'))}</div>${sessions.length?sessions.map(x=>`<div class="session"><div class="time">${x.minutes||0}${t('minutesShort')}</div><div class="session-main"><strong>${esc(x.title)}</strong><span>${course(x.courseId)?.name||''}</span></div><span class="pill ${x.priority==='high'?'warning':''}">${t(x.priority||'medium')}</span><button class="icon-btn" data-action="start-task" data-id="${x.id}" aria-label="${t('startSession')}">${I('play',14)}</button></div>`).join(''):`<div class="empty"><strong>${t('noTasks')}</strong><span>${state.lang==='ar'?'أضف مهمة وسيتولى مسار ترتيبها.':'Add a task and Masar will prioritize it.'}</span></div>`}</div><div class="card health"><div class="section-head"><div><h2>${t('health')}</h2><p>${state.lang==='ar'?'مؤشر سريع لمدى قابلية تنفيذ الخطة.':'A quick view of plan executability.'}</p></div><span class="pill success">${avg>=60?'Good':'Watch'}</span></div><div class="health-score"><strong>${Math.max(0,Math.min(100,Math.round(100-(totalMin>300?(totalMin-300)/8:0))))}%</strong><span class="muted small">${t('weekLoad')}</span></div><div class="bar"><i style="width:${Math.max(8,Math.min(100,Math.round(100-(totalMin>300?(totalMin-300)/8:0))))}%"></i></div><div class="health-row"><span>${state.lang==='ar'?'أقرب امتحان':'Next exam'}</span><span>${ex?fmtDate(ex.date):'—'}</span></div><div class="health-row"><span>${state.lang==='ar'?'مهام عالية':'High priority'}</span><span>${open.filter(x=>x.priority==='high').length}</span></div><div class="health-row"><span>${state.lang==='ar'?'نسخة الخطة':'Plan version'}</span><span>v${state.planVersion}</span></div></div></div></section><section class="section"><div class="section-head"><div><h2>${t('week')}</h2><p>${state.lang==='ar'?'لمحة عن الحمل القادم.':'A compact view of the workload ahead.'}</p></div>${btn('route-planner',t('planner'),'calendar')}</div>${weekMarkup()}</section>`)}
function weekMarkup(){const start=new Date(today);start.setDate(start.getDate()-start.getDay()+1);const names=state.lang==='ar'?['الإث','الثلا','الأرب','الخمي','الجمع','السبت','الأحد']:['Mon','Tue','Wed','Thu','Fri','Sat','Sun'];return `<div class="week">${Array.from({length:7},(_,i)=>{const d=addDays(start,i),tasks=state.tasks.filter(x=>x.date===d&&!x.done);return `<div class="day ${d===iso(today)?'today':''}"><div class="day-name">${names[i]}</div><div class="day-num">${new Date(d).getDate()}</div>${tasks.slice(0,3).map(x=>`<div class="event ${x.priority==='low'?'green':x.priority==='high'?'amber':''}">${esc(x.title)}</div>`).join('')}${tasks.length>3?`<div class="muted" style="font-size:9px;margin-top:5px">+${tasks.length-3}</div>`:''}</div>`}).join('')}</div>`}
function score(x){const p={high:40,medium:20,low:5}[x.priority]||10;const ex=state.exams.find(e=>e.courseId===x.courseId);return p+(ex?Math.max(0,30-daysLeft(ex.date)*2):0)+(x.date===iso(today)?20:0)}
function renderPlanner(){const actions=btn('analysis',t('analysis'),'chart')+btn('rebuild',t('rebuild'),'refresh');shell(`${pageHead(t('planner'),state.lang==='ar'?'الخطة تتغير عندما تتغير ظروفك.':'The plan changes when your reality changes.',actions)}${weekMarkup()}<section class="section"><div class="grid-2"><div class="card list-card"><div class="section-head"><div><h2>${state.lang==='ar'?'تحليل الحمل':'Workload analysis'}</h2><p>${state.lang==='ar'?'أين تحتاج الخطة إلى مساحة؟':'Where the plan needs breathing room.'}</p></div></div>${planAnalysis()}</div><div class="card list-card"><div class="section-head"><div><h2>${state.lang==='ar'?'أولوية الجلسات':'Session priority'}</h2><p>${state.lang==='ar'?'الترتيب الذي سيستخدمه مسار.':'The order Masar will use.'}</p></div></div>${state.tasks.filter(x=>!x.done).sort((a,b)=>score(b)-score(a)).slice(0,6).map((x,i)=>`<div class="session"><div class="time">#${i+1}</div><div class="session-main"><strong>${esc(x.title)}</strong><span>${course(x.courseId)?.name||''} · ${x.minutes}${t('minutesShort')}</span></div><span class="pill ${x.priority==='high'?'warning':''}">${t(x.priority)}</span></div>`).join('')||`<div class="empty">${t('noTasks')}</div>`}</div></div></section>`) }
function planAnalysis(){const open=state.tasks.filter(x=>!x.done),mins=open.reduce((s,x)=>s+x.minutes,0),high=open.filter(x=>x.priority==='high').length;return `<div class="health-row"><span>${t('remaining')}</span><span>${mins} ${t('minutesShort')}</span></div><div class="health-row"><span>${state.lang==='ar'?'عالية الأولوية':'High priority'}</span><span>${high}</span></div><div class="health-row"><span>${state.lang==='ar'?'أقرب امتحان':'Nearest exam'}</span><span>${state.exams.length?fmtDate(state.exams.slice().sort((a,b)=>new Date(a.date)-new Date(b.date))[0].date):'—'}</span></div><div style="margin-top:16px">${mins>300?`<div class="pill warning">${state.lang==='ar'?'الحمل أعلى من مساحة أسبوع مريحة. استخدم الاسترداد.':'Load is above a comfortable weekly capacity. Consider recovery.'}</div>`:`<div class="pill success">${state.lang==='ar'?'الحمل قابل للتنفيذ حاليًا.':'Current load is executable.'}</div>`}</div>`}
function renderCourses(){const actions=btn('add-course',t('addCourse'),'plus');shell(`${pageHead(t('courses'),state.lang==='ar'?'كل مادة لها تقدمها وسياقها.':'Every course keeps its own progress and context.',actions)}<div class="page-grid">${state.courses.map(c=>`<article class="card course-card"><div class="course-head"><div><div class="course-title">${esc(c.name)}</div><div class="course-code">${esc(c.code||'')}</div></div><button class="icon-btn" data-action="edit-course" data-id="${c.id}" aria-label="${t('edit')}">${I('more',16)}</button></div><div class="course-progress"><div class="bar"><i style="width:${c.progress}%"></i></div></div><div class="course-footer"><span>${c.progress}% ${t('progress')}</span><span>${state.exams.filter(e=>e.courseId===c.id).length} ${t('exams')}</span></div></article>`).join('')||`<div class="card empty" style="grid-column:1/-1"><strong>${t('noCourses')}</strong></div>`}</div>`) }
function renderExams(){const actions=btn('add-exam',t('addExam'),'plus');shell(`${pageHead(t('exams'),state.lang==='ar'?'الوقت والجاهزية في مكان واحد.':'Time and readiness in one place.',actions)}<div class="card table-card"><table class="table"><thead><tr><th>${t('title')}</th><th>${t('course')}</th><th>${t('date')}</th><th>${t('readiness')}</th><th>${t('risk')}</th><th></th></tr></thead><tbody>${state.exams.slice().sort((a,b)=>new Date(a.date)-new Date(b.date)).map(e=>{const r=e.readiness<45?'high':e.readiness<70?'mid':'low';return `<tr><td><strong>${esc(e.title)}</strong></td><td>${esc(course(e.courseId)?.name||'—')}</td><td>${fmtDate(e.date)} · ${Math.max(0,daysLeft(e.date))} ${t('days')}</td><td>${e.readiness}%</td><td class="risk ${r}">${t(r==='high'?'high':r==='mid'?'medium':'low')}</td><td><button class="icon-btn" data-action="edit-exam" data-id="${e.id}" aria-label="${t('edit')}">${I('more',15)}</button></td></tr>`}).join('')||`<tr><td colspan="6"><div class="empty"><strong>${t('noExams')}</strong></div></td></tr>`}</tbody></table></div>`) }
function renderTasks(){const actions=btn('add-task',t('addTask'),'plus');const tasks=state.tasks.slice().sort((a,b)=>Number(a.done)-Number(b.done)||score(b)-score(a));shell(`${pageHead(t('tasks'),state.lang==='ar'?'المهام تدخل مباشرة في ترتيب الخطة.':'Tasks feed directly into plan priority.',actions)}<div class="card list-card">${tasks.map(x=>`<div class="task"><button class="check ${x.done?'done':''}" data-action="toggle-task" data-id="${x.id}" aria-label="${t('done')}">${x.done?I('check',14):''}</button><div class="task-body ${x.done?'done':''}"><strong>${esc(x.title)}</strong><div class="task-meta">${course(x.courseId)?.name||'—'} · ${x.minutes}${t('minutesShort')} · ${fmtDate(x.date)}</div></div><span class="pill ${x.priority==='high'?'warning':''}">${t(x.priority)}</span><button class="icon-btn" data-action="edit-task" data-id="${x.id}" aria-label="${t('edit')}">${I('more',15)}</button></div>`).join('')||`<div class="empty"><strong>${t('noTasks')}</strong></div>`}</div>`) }
function renderProgress(){const avg=state.courses.length?Math.round(state.courses.reduce((s,x)=>s+x.progress,0)/state.courses.length):0;const mins=state.sessions.reduce((s,x)=>s+x.minutes,0);shell(`${pageHead(t('progress'),state.lang==='ar'?'قياس بسيط لما يتحرك فعليًا.':'Measure what actually moves.',btn('export',state.lang==='ar'?'تصدير التقرير':'Export report','download'))}<div class="grid-4"><div class="card stat-card"><div class="stat-top"><span>${t('progress')}</span>${I('chart',16)}</div><div class="stat-value">${avg}%</div><div class="stat-note">${state.lang==='ar'?'متوسط المواد':'Course average'}</div></div><div class="card stat-card"><div class="stat-top"><span>${t('minutesDone')}</span>${I('focus',16)}</div><div class="stat-value">${mins}</div><div class="stat-note">${state.sessions.length} ${t('sessions')}</div></div><div class="card stat-card"><div class="stat-top"><span>${t('completed')}</span>${I('check',16)}</div><div class="stat-value">${state.tasks.filter(x=>x.done).length}</div><div class="stat-note">${state.tasks.length} ${state.lang==='ar'?'مهمة':'tasks'}</div></div><div class="card stat-card"><div class="stat-top"><span>${state.lang==='ar'?'الخطة':'Plan'}</span>${I('refresh',16)}</div><div class="stat-value">v${state.planVersion}</div><div class="stat-note">${state.lang==='ar'?'نسخة الخطة':'plan version'}</div></div></div><section class="section"><div class="card list-card"><div class="section-head"><div><h2>${state.lang==='ar'?'تقدم المواد':'Course progress'}</h2><p>${state.lang==='ar'?'التقدم الذي أدخلته أنت، وليس رقمًا تجميليًا.':'Progress you entered, not vanity metrics.'}</p></div></div>${state.courses.map(c=>`<div class="session"><div class="session-main"><strong>${esc(c.name)}</strong><span>${c.progress}%</span></div><div style="width:42%"><div class="bar"><i style="width:${c.progress}%"></i></div></div></div>`).join('')}</div></section>`) }
function renderFocus(){const active=focus.taskId?state.tasks.find(x=>x.id===focus.taskId):null;if(!active){const candidates=state.tasks.filter(x=>!x.done).sort((a,b)=>score(b)-score(a));shell(`<div class="focus-screen"><div class="icon-tile" style="margin:auto;width:48px;height:48px">${I('focus',22)}</div><h1>${t('focusTitle')}</h1><p class="muted">${t('focusDesc')}</p><div class="card list-card" style="max-width:600px;margin:25px auto;text-align:start">${candidates.slice(0,5).map(x=>`<div class="session"><div class="session-main"><strong>${esc(x.title)}</strong><span>${course(x.courseId)?.name||''} · ${x.minutes}${t('minutesShort')}</span></div>${btn('focus-task',t('startSession'),'play').replace('<button','<button data-id="'+x.id+'"')}</div>`).join('')||`<div class="empty"><strong>${t('noTasks')}</strong></div>`}</div></div>`);return}const mm=String(Math.floor(focus.remaining/60)).padStart(2,'0'),ss=String(focus.remaining%60).padStart(2,'0'),pct=Math.round((1-focus.remaining/focus.total)*100);shell(`<div class="focus-screen"><div class="focus-subject">${esc(course(active.courseId)?.name||'')} · ${t('focusTitle')}</div><h1>${esc(active.title)}</h1><div class="timer">${mm}:${ss}</div><div class="focus-progress"><div class="bar"><i style="width:${pct}%"></i></div><div class="muted small" style="margin-top:7px">${pct}%</div></div><div class="focus-actions">${btn(focus.running?'focus-pause':'focus-resume',focus.running?t('pause'):t('resume'),focus.running?'pause':'play')}${btn('focus-finish',t('finish'),'check')}${btn('focus-exit',t('back'),'close')}</div></div>`) }
function renderSettings(){shell(`${pageHead(t('settingsTitle'),state.lang==='ar'?'غيّر سلوك مسار بدون مغادرة الصفحة.':'Change Masar behavior without leaving the page.') }<div class="card settings-list"><div class="setting"><div><strong>${t('language')}</strong><span>${state.lang==='ar'?t('arabic'):t('english')}</span></div><button class="btn" data-action="language">${state.lang==='ar'?t('english'):t('arabic')}</button></div><div class="setting"><div><strong>${t('theme')}</strong><span>${state.theme==='dark'?t('dark'):t('light')}</span></div><button class="btn" data-action="theme">${state.theme==='dark'?t('light'):t('dark')}</button></div><div class="setting"><div><strong>${t('adaptive')}</strong><span>${t('adaptiveDesc')}</span></div><button class="switch ${state.settings.adaptive?'on':''}" data-action="adaptive" aria-label="${t('adaptive')}"><i></i></button></div><div class="setting"><div><strong>${t('notifications')}</strong><span>${t('notificationsDesc')}</span></div><button class="switch ${state.settings.notifications?'on':''}" data-action="notifications-toggle" aria-label="${t('notifications')}"><i></i></button></div><div class="setting"><div><strong>${state.lang==='ar'?'مسح البيانات المحلية':'Reset local data'}</strong><span>${state.lang==='ar'?'يحذف بيانات هذا المتصفح ويعيد البيانات التجريبية.':'Delete browser data and restore demo data.'}</span></div><button class="btn danger-btn" data-action="reset">${state.lang==='ar'?'إعادة ضبط':'Reset'}</button></div></div>`) }
function modal(title,desc,body,foot){document.getElementById('modal-root').innerHTML=`<div class="modal-backdrop" data-action="close-modal"><section class="modal" role="dialog" aria-modal="true" aria-label="${esc(title)}" data-modal><div class="modal-head"><div><h3>${esc(title)}</h3><p>${esc(desc||'')}</p></div><button class="icon-btn" data-action="close-modal" aria-label="${t('close')}">${I('close',16)}</button></div><div class="modal-body">${body}</div><div class="modal-foot">${foot}</div></section></div>`;const first=document.querySelector('#modal-root input,#modal-root select,#modal-root textarea');first?.focus()}
function closeModal(){document.getElementById('modal-root').innerHTML=''}
function courseOptions(selected=''){return state.courses.map(c=>`<option value="${c.id}" ${c.id===selected?'selected':''}>${esc(c.name)}</option>`).join('')}
function openCourse(id=null){const c=id?course(id):{name:'',code:'',progress:0};modal(id?t('edit')+' '+t('course'):t('addCourse'),state.lang==='ar'?'المادة تصبح جزءًا من الخطة فور حفظها.':'The course becomes part of planning immediately.',`<div class="form-grid"><div class="field"><label>${t('name')}</label><input id="f-name" value="${esc(c.name)}" maxlength="80" required></div><div class="field"><label>Code</label><input id="f-code" value="${esc(c.code||'')}" maxlength="20"></div><div class="field full"><label>${t('progress')} (0–100)</label><input id="f-progress" type="number" min="0" max="100" value="${c.progress}"><div class="hint">${state.lang==='ar'?'مثال: 42':'Example: 42'}</div></div></div>`,`${id?`<button class="btn danger-btn" data-action="delete-course" data-id="${id}">${t('delete')}</button>`:''}<span style="flex:1"></span><button class="btn" data-action="close-modal">${t('cancel')}</button><button class="btn btn-primary" data-action="save-course" data-id="${id||''}">${t('save')}</button>`)}
function openExam(id=null){const e=id?state.exams.find(x=>x.id===id):{title:'',date:iso(today),courseId:state.courses[0]?.id||'',readiness:0};modal(id?t('edit')+' '+t('exam'):t('addExam'),state.lang==='ar'?'حدد التاريخ والجاهزية بوضوح.':'Set the date and readiness clearly.',`<div class="form-grid"><div class="field full"><label>${t('title')}</label><input id="f-title" value="${esc(e.title)}" maxlength="100" required></div><div class="field"><label>${t('date')}</label><input id="f-date" type="date" value="${e.date}" required></div><div class="field"><label>${t('course')}</label><select id="f-course">${courseOptions(e.courseId)}</select></div><div class="field full"><label>${t('readiness')} <span class="muted">(0–100%)</span></label><input id="f-readiness" type="number" min="0" max="100" value="${Math.max(0,Math.min(100,e.readiness))}" required></div></div>`,`${id?`<button class="btn danger-btn" data-action="delete-exam" data-id="${id}">${t('delete')}</button>`:''}<span style="flex:1"></span><button class="btn" data-action="close-modal">${t('cancel')}</button><button class="btn btn-primary" data-action="save-exam" data-id="${id||''}">${t('save')}</button>`)}
function openTask(id=null){const x=id?state.tasks.find(x=>x.id===id):{title:'',date:iso(today),courseId:state.courses[0]?.id||'',minutes:30,priority:'medium'};modal(id?t('edit')+' '+t('task'):t('addTask'),state.lang==='ar'?'المهمة تدخل مباشرة في ترتيب الأولويات.':'The task feeds directly into priority.',`<div class="form-grid"><div class="field full"><label>${t('title')}</label><input id="f-title" value="${esc(x.title)}" maxlength="120" required></div><div class="field"><label>${t('course')}</label><select id="f-course">${courseOptions(x.courseId)}</select></div><div class="field"><label>${t('date')}</label><input id="f-date" type="date" value="${x.date}"></div><div class="field"><label>${t('minutes')}</label><input id="f-minutes" type="number" min="5" max="600" step="5" value="${x.minutes}"></div><div class="field"><label>${t('priority')}</label><select id="f-priority"><option value="high" ${x.priority==='high'?'selected':''}>${t('high')}</option><option value="medium" ${x.priority==='medium'?'selected':''}>${t('medium')}</option><option value="low" ${x.priority==='low'?'selected':''}>${t('low')}</option></select></div></div>`,`${id?`<button class="btn danger-btn" data-action="delete-task" data-id="${id}">${t('delete')}</button>`:''}<span style="flex:1"></span><button class="btn" data-action="close-modal">${t('cancel')}</button><button class="btn btn-primary" data-action="save-task" data-id="${id||''}">${t('save')}</button>`)}
function openRecovery(){let selected='redistribute';modal(t('recoveryTitle'),t('recoveryDesc'),`<div class="recovery-options">${[['redistribute','redistribute','redistributeDesc'],['defer','defer','deferDesc'],['extend','extend','extendDesc']].map(([v,a,b],i)=>`<button class="recovery-option ${i===0?'selected':''}" data-recovery="${v}"><i class="radio"></i><div><strong>${t(a)}</strong><span>${t(b)}</span></div></button>`).join('')}</div>`,`${btn('close-modal',t('cancel'))}<button class="btn btn-primary" data-action="apply-recovery">${t('apply')}</button>`);document.querySelectorAll('[data-recovery]').forEach(el=>el.addEventListener('click',()=>{document.querySelectorAll('[data-recovery]').forEach(x=>x.classList.remove('selected'));el.classList.add('selected');selected=el.dataset.recovery}));document.getElementById('modal-root').dataset.recovery=selected}
function startFocus(id){const x=state.tasks.find(t=>t.id===id);if(!x)return;focus={taskId:id,total:(x.minutes||25)*60,remaining:(x.minutes||25)*60,running:true,timer:null};route='focus';render();tick()}
function tick(){clearInterval(focus.timer);focus.timer=setInterval(()=>{if(!focus.running)return;focus.remaining--;if(focus.remaining<=0){clearInterval(focus.timer);finishFocus(true);return}render()},1000)}
function finishFocus(auto=false){const x=state.tasks.find(t=>t.id===focus.taskId);if(!x)return;const done=Math.max(1,Math.round((focus.total-focus.remaining)/60));state.sessions.push({id:uid(),taskId:x.id,title:x.title,courseId:x.courseId,minutes:done,date:iso(today)});if(focus.remaining<=0)x.done=true;save();clearInterval(focus.timer);focus={taskId:null,remaining:0,total:0,timer:null,running:false};route='focus';render();toast(auto?(state.lang==='ar'?'انتهت الجلسة وسُجلت.':'Session complete and saved.'):t('saved'))}
function rebuild(){const open=state.tasks.filter(x=>!x.done).sort((a,b)=>score(b)-score(a));let cursor=new Date(today);let used=0;open.forEach(x=>{if(used+(x.minutes||0)>180){cursor.setDate(cursor.getDate()+1);used=0}x.date=iso(cursor);used+=x.minutes||0});state.planVersion++;save();render();toast(t('planUpdated'))}
function applyRecovery(){const selected=document.getElementById('modal-root').dataset.recovery||'redistribute';const open=state.tasks.filter(x=>!x.done);if(selected==='defer'){open.forEach(x=>{if(x.priority==='low')x.date=addDays(x.date,2)})}else if(selected==='extend'){const max=Math.max(...open.map(x=>new Date(x.date).getTime()),new Date(today).getTime());open.filter(x=>x.priority!=='low').forEach(x=>{if(new Date(x.date).getTime()<max)x.date=addDays(x.date,1)})}else{let cursor=new Date(today),used=0;open.sort((a,b)=>score(b)-score(a)).forEach(x=>{if(used+x.minutes>180){cursor.setDate(cursor.getDate()+1);used=0}x.date=iso(cursor);used+=x.minutes})}state.planVersion++;save();closeModal();render();toast(t('planUpdated'))}
function exportReport(){const text=`MASAR — ${new Date().toLocaleDateString()}\n\nCourses:\n${state.courses.map(c=>`- ${c.name}: ${c.progress}%`).join('\n')}\n\nTasks completed: ${state.tasks.filter(x=>x.done).length}/${state.tasks.length}\nFocus minutes: ${state.sessions.reduce((s,x)=>s+x.minutes,0)}\nPlan version: ${state.planVersion}`;const a=document.createElement('a');a.href=URL.createObjectURL(new Blob([text],{type:'text/plain'}));a.download='masar-progress.txt';a.click();URL.revokeObjectURL(a.href);toast(state.lang==='ar'?'تم تصدير التقرير':'Report exported')}
function searchModal(){modal(t('search'),state.lang==='ar'?'ابحث داخل المواد والامتحانات والمهام.':'Search courses, exams and tasks.',`<div class="field"><input id="search-input" placeholder="${esc(t('search'))}" autocomplete="off"></div><div id="search-results" style="margin-top:12px"></div>`,btn('close-modal',t('close')));const input=document.getElementById('search-input');const draw=()=>{const q=input.value.trim().toLowerCase();const arr=[...state.courses.map(x=>({type:t('courses'),title:x.name,route:'courses'})),...state.exams.map(x=>({type:t('exams'),title:x.title,route:'exams'})),...state.tasks.map(x=>({type:t('tasks'),title:x.title,route:'tasks'}))].filter(x=>!q||x.title.toLowerCase().includes(q));document.getElementById('search-results').innerHTML=arr.slice(0,8).map(x=>`<button class="recovery-option" data-route="${x.route}" style="width:100%;margin-top:6px"><div><strong>${esc(x.title)}</strong><span>${esc(x.type)}</span></div></button>`).join('')||`<div class="empty">${t('searchEmpty')}</div>`};input.addEventListener('input',draw);draw()}
function handle(action,id){if(action==='close-modal'){closeModal();return}if(action==='add-course')openCourse();else if(action==='edit-course')openCourse(id);else if(action==='save-course'){const name=document.getElementById('f-name').value.trim();if(!name)return toast(t('invalid'));const p=Math.max(0,Math.min(100,Number(document.getElementById('f-progress').value)||0));if(id){const c=course(id);Object.assign(c,{name,code:document.getElementById('f-code').value.trim(),progress:p})}else state.courses.push({id:uid(),name,code:document.getElementById('f-code').value.trim(),progress:p});save();closeModal();render();toast(t('saved'))}else if(action==='delete-course'){if(!confirm(t('confirmDelete')))return;state.courses=state.courses.filter(x=>x.id!==id);state.tasks=state.tasks.map(x=>x.courseId===id?{...x,courseId:null}:x);state.exams=state.exams.map(x=>x.courseId===id?{...x,courseId:null}:x);save();closeModal();render();toast(t('deleted'))}else if(action==='add-exam')openExam();else if(action==='edit-exam')openExam(id);else if(action==='save-exam'){const title=document.getElementById('f-title').value.trim(),date=document.getElementById('f-date').value,courseId=document.getElementById('f-course').value,readiness=Math.max(0,Math.min(100,Number(document.getElementById('f-readiness').value)||0));if(!title||!date||!courseId)return toast(t('invalid'));const obj={title,date,courseId,readiness};if(id)Object.assign(state.exams.find(x=>x.id===id),obj);else state.exams.push({id:uid(),...obj});save();closeModal();render();toast(t('saved'))}else if(action==='delete-exam'){if(!confirm(t('confirmDelete')))return;state.exams=state.exams.filter(x=>x.id!==id);save();closeModal();render();toast(t('deleted'))}else if(action==='add-task')openTask();else if(action==='edit-task')openTask(id);else if(action==='save-task'){const title=document.getElementById('f-title').value.trim(),courseId=document.getElementById('f-course').value,date=document.getElementById('f-date').value,minutes=Math.max(5,Math.min(600,Number(document.getElementById('f-minutes').value)||30)),priority=document.getElementById('f-priority').value;if(!title||!date||!courseId)return toast(t('invalid'));const obj={title,courseId,date,minutes,priority};if(id)Object.assign(state.tasks.find(x=>x.id===id),obj);else state.tasks.push({id:uid(),...obj,done:false});save();closeModal();render();toast(t('saved'))}else if(action==='delete-task'){if(!confirm(t('confirmDelete')))return;state.tasks=state.tasks.filter(x=>x.id!==id);save();closeModal();render();toast(t('deleted'))}else if(action==='toggle-task'){const x=state.tasks.find(x=>x.id===id);if(x)x.done=!x.done;save();render();toast(t('saved'))}else if(action==='start-task'||action==='focus-task'||action==='primary-start'){startFocus(id||state.tasks.filter(x=>!x.done).sort((a,b)=>score(b)-score(a))[0]?.id)}else if(action==='recover')openRecovery();else if(action==='apply-recovery')applyRecovery();else if(action==='rebuild')rebuild();else if(action==='analysis'){toast(state.lang==='ar'?'التحليل ظاهر أسفل الخطة.':'Analysis is shown below the plan.')}else if(action==='route-planner'){route='planner';render()}else if(action==='export')exportReport();else if(action==='search')searchModal();else if(action==='notifications'){const e=state.exams.slice().sort((a,b)=>daysLeft(a.date)-daysLeft(b.date))[0];modal(t('notifications'),t('notificationsDesc'),e?`<div class="notification"><strong>${esc(e.title)}</strong><p>${t('examSoon')} · ${Math.max(0,daysLeft(e.date))} ${t('days')} · ${e.readiness}% ${t('readiness')}</p></div>`:`<div class="empty">${t('noData')}</div>`,btn('close-modal',t('close')))}else if(action==='theme'){state.theme=state.theme==='dark'?'light':'dark';save();render()}else if(action==='language'){state.lang=state.lang==='ar'?'en':'ar';save();closeModal();render()}else if(action==='adaptive'){state.settings.adaptive=!state.settings.adaptive;save();render();toast(t('saved'))}else if(action==='notifications-toggle'){state.settings.notifications=!state.settings.notifications;save();render();toast(t('saved'))}else if(action==='reset'){if(confirm(t('confirmDelete'))){state=structuredClone(demo);save();route='today';render();toast(t('saved'))}}else if(action==='focus-pause'){focus.running=false;render()}else if(action==='focus-resume'){focus.running=true;tick();render()}else if(action==='focus-finish')finishFocus(false);else if(action==='focus-exit'){if(confirm(state.lang==='ar'?'الخروج الآن؟ لن يتم تسجيل جلسة غير مكتملة.':'Exit now? The incomplete session will not be saved.')){clearInterval(focus.timer);focus={taskId:null,remaining:0,total:0,timer:null,running:false};render()}}}
document.addEventListener('click',e=>{const routeEl=e.target.closest('[data-route]');if(routeEl){route=routeEl.dataset.route;closeModal();render();return}const a=e.target.closest('[data-action]');if(a){if(a.dataset.action==='close-modal'&&e.target.closest('[data-modal]')&&!e.target.closest('button'))return;handle(a.dataset.action,a.dataset.id);return}});
document.addEventListener('keydown',e=>{if((e.metaKey||e.ctrlKey)&&e.key.toLowerCase()==='k'){e.preventDefault();searchModal()}if(e.key==='Escape'&&document.getElementById('modal-root').innerHTML)closeModal()});
window.MASAR={getState:()=>state,setState:(next)=>{state=next;save();render()},render,save,toast,modal,closeModal,t,course,iso,addDays};
render();
